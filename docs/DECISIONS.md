# Relive — Architecture Decision Record

This is the running log of **major** architectural and product-structure decisions. Record a decision here when it settles something future work must respect. Once recorded, a decision is **settled**: do not silently change it (see [`../AGENTS.md`](../AGENTS.md)). To change a settled decision, add a new entry that supersedes the old one and explains why.

Format for each entry:

- **ID / date / status** (Accepted | Superseded | Proposed)
- **Context** — the situation forcing a choice
- **Decision** — what was chosen
- **Consequences** — what this enables/constrains

---

## ADR-0100 — Media replaces All Photos and includes every attachment type

- **Date:** 2026-09-24 · **Status:** Accepted · supersedes ADR-0098 and ADR-0099 where they limit the collection or save action to visual media
- **Context:** The visual-only All Photos destination omitted voice recordings and its inline selection row did not match Relive's pinned contextual-action language. Saving also lacked visible cancellable progress.
- **Decision:** The user-facing collection is named `Media` and flattens image, video, and audio attachments in stable Moment/attachment order. Long-press replaces the pinned Back control with Close, a centered selected count, and Download. Saving presents a modal Material 3 determinate loader over blurred content with Cancel; completion, partial failure, cancellation, or failure is reported by the bottom snackbar. Android saves images, videos, and audio through MediaStore into type-appropriate Relive folders. Playback controls use semantic theme colors, including composer drafts before Keep Moment.
- **Consequences:** The existing cover and curved sheet partition remain unchanged. The collection stays derived and read-only with no new persistence. iOS Photos can save images and videos; unsupported audio is reported as a partial failure.

## ADR-0098 — All Photos opens as a visual-media gallery

- **Date:** 2026-09-24 · **Status:** Accepted · supersedes ADR-0061/ADR-0065 only for the All Photos sheet contents
- **Context:** Reusing the generic read-only collection timeline made All Photos feel like another Moment feed, even though its purpose is direct visual browsing. The existing per-Moment multi-media gallery already establishes the approved tile and viewer behavior.
- **Decision:** The All Photos Home card and sliding-cover transition remain unchanged. Beneath that cover, the destination flattens eligible image and video attachments from its derived collection in stable Moment/attachment order and renders them with the multi-media gallery tile presentation. It renders no Moment cards or timeline rail. Tapping a tile opens the existing full-screen viewer at that exact index, with horizontal paging across the flattened visual collection. Audio is excluded because All Photos is a visual collection.
- **Consequences:** The destination remains read-only and adds no persistence or dependency. The shared gallery tile stays the single visual implementation for per-Moment and All Photos grids, while the full-screen viewer retains playback, zoom, and Back behavior. Other Rediscover collections keep their Moment-based presentations.

---

## ADR-0060 — Rediscover collection navigation uses fade-through, not a container transform · reaffirmed by ADR-0063

- **Date:** 2026-08-31 · **Status:** Accepted · scope narrowed by ADR-0061 · superseded for card-opened collection routes by ADR-0065, whose cover retention satisfies this ADR's own condition
- **Context:** The Rediscover card cover is not retained in the read-only collection screen. A container transform therefore created a false spatial continuity: the cover moved toward the destination before the collection surface appeared.
- **Decision:** Favorites, On This Day, and From Your Past collection navigation uses the M3 fade-through pattern. The source exits with `short4` emphasized-accelerate; the destination enters after that with `medium2` emphasized-decelerate. Reduced motion uses the theme helper's short standard fade.
- **Consequences:** Collection navigation remains purposeful and accessible without implying that a card cover is a persistent element of the destination. The Phase 3.4 container-transform instruction is superseded for this route. Per ADR-0061 this pattern governs the read-only collection routes opened from the Home surface's Rediscover row — Favourites, On This Day, From Your Past, and All Photos — and no longer applies to reaching All moments, which is a scroll on the same surface rather than a route.

---

## ADR-0001 — Local-first, no backend

- **Date:** 2026-08-20 · **Status:** Accepted
- **Context:** Relive is a private personal memory archive. The product explicitly excludes backend, cloud sync, login, and social features for the current product.
- **Decision:** All data is stored on-device. No server, network data layer, authentication, or sync is built now.
- **Consequences:** Persistence is a local database plus on-device media files. No networking stack or auth. Any future sync would be a separate, explicitly approved decision. See [`ARCHITECTURE.md`](ARCHITECTURE.md) §1, §5.

---

## ADR-0002 — Kotlin Multiplatform + Compose Multiplatform, shared UI

- **Date:** 2026-08-20 · **Status:** Accepted
- **Context:** Target Android and iOS with a small team and a distinctive, consistent UI.
- **Decision:** Use Kotlin Multiplatform for shared logic and Compose Multiplatform for shared UI. Platform apps (`androidApp`, `iosApp`) are thin entry points. Toolchain is pinned in `gradle/libs.versions.toml` (Kotlin 2.4.10, Compose MP 1.11.1, Material3 1.11.0-alpha07, AGP 9.0.1).
- **Consequences:** UI and business logic live in `shared/`. Platform code is minimized. See [`ARCHITECTURE.md`](ARCHITECTURE.md) §2–§3.

---

## ADR-0003 — Material 3 foundation, custom Relive design system on top

- **Date:** 2026-08-20 · **Status:** Accepted
- **Context:** The app must be accessible and behave correctly, but must **not** look like a default Material app. It must match the approved warm-editorial reference.
- **Decision:** Build on Material 3 for interaction, accessibility, and component behavior; apply a strict tokenized Relive design system for all visual styling. The approved UI reference in `docs/ui-reference/` is authoritative for timeline UI.
- **Consequences:** No raw styling in components — everything comes from tokens. When tokens conflict with the reference, work stops and the conflict is reported. See [`DESIGN_SYSTEM.md`](DESIGN_SYSTEM.md). Per ADR-0061 that authority covers Relive's warm editorial visual identity — Moment card layout, timeline rail and dots, type, color, and spacing — and not the Home surface's composition, entry scroll position, or navigation structure, which ADR-0061 settles.

---

## ADR-0004 — Store each moment once; timelines reference moments

- **Date:** 2026-08-20 · **Status:** Accepted
- **Context:** A moment can appear in multiple timelines; duplicating it would corrupt edits and waste storage.
- **Decision:** Persist each moment exactly once. Custom timelines reference moments via a many-to-many membership table. Attachments and tags reference their moment. The built-in **All** timeline is a logical query over all moments, not per-moment membership rows.
- **Consequences:** Editing/forgetting a moment affects all timelines consistently. Membership is cheap to change. See [`PRODUCT_SPEC.md`](PRODUCT_SPEC.md) §2, §13 and [`ARCHITECTURE.md`](ARCHITECTURE.md) §5.

---

## ADR-0005 — 4-day edit/forget window keyed on immutable `createdAt`

- **Date:** 2026-08-20 · **Status:** Accepted
- **Context:** Memories should feel permanent shortly after capture, with a brief correction window, and must not be endlessly editable.
- **Decision:** A moment may be edited or forgotten only within 4 days of its **immutable `createdAt`**. `updatedAt` **never** extends the window. The rule is centralized in the domain layer using a `Clock` abstraction and governs long-press actions, inline editing, and forgetting. Forgetting requires confirmation.
- **Consequences:** Deterministic, testable rule. UI must hide Edit/Forget after the window closes. See [`PRODUCT_SPEC.md`](PRODUCT_SPEC.md) §8 and [`TESTING.md`](TESTING.md).

---

## ADR-0006 — Timeline-scoped search

- **Date:** 2026-08-20 · **Status:** Accepted
- **Context:** Users navigate by timeline (chapter of life); search should respect the current context.
- **Decision:** Search always operates within the currently selected timeline, with All / Tags / Places filters scoped to that timeline. On the built-in All timeline, scope is every moment. All-search behaves like WhatsApp chat search (highlight, match count, up/down navigation, auto-scroll).
- **Consequences:** Search queries carry the active timeline scope. Places/Tags suggestions derive only from the current timeline's moments. See [`PRODUCT_SPEC.md`](PRODUCT_SPEC.md) §9.

---

## ADR-0007 — Platform capabilities behind shared interfaces

- **Date:** 2026-08-20 · **Status:** Accepted
- **Context:** Location, media capture/storage, and similar capabilities differ per platform, but shared logic must stay platform-agnostic.
- **Decision:** Declare capability interfaces in shared code (e.g. `LocationProvider`, `PlaceResolver`, media capture/storage). Implement them in `androidMain` (Android APIs) and `iosMain` (Core Location, AVFoundation/UIKit). Shared domain/presentation code depends only on the interfaces and never imports platform APIs.
- **Consequences:** Business logic is testable with fakes; implementations can change without touching shared logic. See [`ARCHITECTURE.md`](ARCHITECTURE.md) §6.

---

## ADR-0008 — Location is moment-scoped and local-only

- **Date:** 2026-08-20 · **Status:** Accepted
- **Context:** Location adds value to a moment but must not become tracking or a privacy liability.
- **Decision:** Location is captured only at moment-creation time, on demand, via `LocationProvider` → `PlaceResolver`. No background tracking, no separate location history, no third-party location analytics. Permission is requested only when needed via the platform flow; denial never blocks saving. All failure states (denied, permanently denied, services disabled, unavailable, timeout) let the composer continue. `ReliveLocation` fields are all optional; raw coordinates are never shown in the normal timeline UI. Location data stays local with the moment. The location data model and interfaces are established **before** platform implementations.
- **Consequences:** Strong privacy posture; robust composer behavior. See [`PRODUCT_SPEC.md`](PRODUCT_SPEC.md) §7 and [`ARCHITECTURE.md`](ARCHITECTURE.md) §6.1.

---

## ADR-0009 — Monetization deferred but seam kept open

- **Date:** 2026-08-20 · **Status:** Accepted
- **Context:** RevenueCat (Pro), RevenueCat Funnels, and Stripe (web) are planned later but must not be built now.
- **Decision:** Represent Pro entitlement behind an interface in shared code, initially backed by a local stub. No RevenueCat/Stripe dependencies are added until the monetization phase. Gating logic lives in domain/presentation so a real source can be swapped in.
- **Consequences:** The app ships free-tier now and can add real entitlements later without restructuring. See [`ARCHITECTURE.md`](ARCHITECTURE.md) §9, [`ROADMAP.md`](ROADMAP.md) Phase 10, [`RELEASE.md`](RELEASE.md).

---

## ADR-0010 — Relive design tokens exposed via `ReliveTheme`, layered on Material 3

- **Date:** 2026-08-20 · **Status:** Accepted
- **Context:** Phase 0 must establish a reusable visual foundation that (a) keeps Material 3 for accessibility/component semantics, (b) forces every visual value to come from a named token, and (c) leaves room for future per-timeline themes (Warm Journal, Monochrome Archive, Film Memory) without rewriting components.
- **Decision:** Introduce a `ReliveThemeTokens` bundle (colors, typography, dimensions, motion) selected by `ReliveThemeId`. `ReliveTheme` composable resolves the active bundle, publishes it through a `staticCompositionLocalOf`, and derives a minimal Material 3 `ColorScheme` + `Typography` from it so `MaterialTheme.*` reads stay valid. Components read design values exclusively via `ReliveTheme.colors / typography / dimensions / motion`; no raw colors, sizes, or durations in UI code. Warm Journal is the only concrete token set in Phase 0; `MonochromeArchive` and `FilmMemory` are declared ids that currently resolve to Warm Journal until their palettes are transcribed.
- **Consequences:** Adding a new theme is a token-set addition plus a `when` branch — no component changes. Non-color tokens (spacing, radii, icons, stroke, timeline, media, opacity, motion) are shared, preventing per-screen drift. Material 3 semantics/accessibility are preserved. See [`DESIGN_SYSTEM.md`](DESIGN_SYSTEM.md) and [`ARCHITECTURE.md`](ARCHITECTURE.md) §7–§8.

---

## ADR-0011 — Playfair Display + Inter bundled locally under the Compose Multiplatform resources system

- **Date:** 2026-08-20 · **Status:** Accepted
- **Context:** `DESIGN_SYSTEM.md` §8 requires Playfair Display (serif) and Inter (sans) to be the actual families used at runtime, with no network font loading. Phase 0 must ship the completed design-system foundation, so temporary platform-family fallbacks are not acceptable.
- **Decision:** Both families are bundled as static TTF binaries inside the design-system layer under `shared/src/commonMain/composeResources/font/`, exposed to Compose only through the internal `rememberReliveSerifFamily()` / `rememberReliveSansFamily()` helpers. `ReliveTheme` composes the typography scale from those families and publishes it via `ReliveTheme.typography`. Only the weights/styles actually referenced by the token mappings are bundled: Playfair Display Regular + Italic, and Inter Regular + Italic + Medium + SemiBold. Both fonts are licensed under SIL Open Font License 1.1; the full license texts are checked into the repository at `shared/licenses/fonts/`. Neither the resource filenames nor the generated `Res` class are exposed outside the `com.vaibhav.relive.ui.theme` package — the Compose resources module is generated with `publicResClass = false`.
- **Consequences:** Visual fidelity to the approved UI reference is achieved without any runtime network dependency. Adding a new typography weight is a design-system-layer change: bundle the TTF, add one `Font(...)` entry in `ReliveFonts.kt`. Redistribution obligations are satisfied by shipping the OFL text alongside the binaries. If a future theme (Monochrome Archive, Film Memory) needs different families, they are declared the same way and injected via `ReliveTheme` — components remain unchanged.

---

## ADR-0012 — Moment minimum validity: at least one of title, content, or media

- **Date:** 2026-08-20 · **Status:** Accepted
- **Context:** PRODUCT_SPEC §4 lists title, content, media, tags, and location as moment fields and confirms text-only moments are supported (§4). It does not explicitly define whether a moment with an empty title AND empty content AND zero attachments is valid. Such a moment would render as a bare timeline dot with nothing to relive, which contradicts the product's "beautiful personal life archive" feeling.
- **Decision:** A moment is semantically valid only if at least one of the following is non-empty: title (non-blank), content (non-blank), or attachments (≥ 1). This rule is enforced in the domain layer by `MomentValidation.validate` and returns a structured result (`Ok` / `Invalid(reasons)`). Structural invariants (unique attachment ids/sortIndex, no duplicate tags in canonical form, `updatedAt >= createdAt`, coordinates present as a pair) are enforced in the constructors of `Moment`, `MediaAttachment`, and `ReliveLocation`. User-entered title/content is preserved verbatim; the only trimming performed is on tag input, and the original label is retained for display.
- **Consequences:** The composer / presentation layer can rely on `MomentValidation` to decide whether a "Keep Moment" tap is allowed. Media-only and text-only moments both remain valid. If PRODUCT_SPEC is later refined to permit truly empty moments (or to add further required fields), this ADR is superseded by a new one.

---

## ADR-0013 — SQLDelight + SQLite as the local persistence engine

- **Date:** 2026-08-20 · **Status:** Accepted
- **Context:** Phase 1B needs a production-quality local database for Android and iOS with a shared schema and query definitions in `commonMain`. Requirements: normalized relational schema (moments once, timelines reference moments, many-to-many memberships, ordered attachments, tags queryable per-timeline), foreign-key enforcement, atomic aggregate writes, no platform APIs leaking into `domain`, no cloud/backend, no runtime code generation. The engine must run on Android (`AndroidSqliteDriver`), iOS (`NativeSqliteDriver` via Kotlin/Native SQLite), and on the JVM host (`JdbcSqliteDriver`) for `androidHostTest`.
- **Decision:** Adopt **SQLDelight 2.1.0** with SQLite as the storage engine. The schema and queries live as `.sq` files in `shared/src/commonMain/sqldelight/com/vaibhav/relive/data/local/db/`; SQLDelight generates typed `ReliveDatabase` + `*Queries` classes at build time under package `com.vaibhav.relive.data.local.db`. Driver construction is platform-specific behind an `expect class DatabaseDriverFactory` (Android actual takes a `Context`, iOS actual is parameterless) and every driver flows through `ReliveDatabaseFactory.create(driver)`, which unconditionally runs `PRAGMA foreign_keys = ON` before returning the typed database. Repositories (`SqlDelightMomentRepository`, `SqlDelightTimelineRepository`, `SqlDelightTagRepository`) implement domain-owned interfaces in `com.vaibhav.relive.domain.repository`; `domain` code depends on no SQLDelight-generated types. `kotlinx.coroutines` is added to expose `Flow`-based observers via SQLDelight's `coroutines-extensions`.
- **Schema decisions:**
  - **Six tables** — `moments`, `custom_timelines`, `moment_timeline_memberships`, `tags`, `moment_tags`, `media_attachments`. `Timeline.All` is never stored — it is served by `MomentRepository.listAll()`; the API rejects `All` as a membership target.
  - **Foreign keys** — enabled at connection level. `moment_timeline_memberships`, `moment_tags`, and `media_attachments` cascade on moment delete. `custom_timelines` cascade on membership only; deleting a custom timeline never deletes moments. `moment_tags → tags(canonical)` uses `ON DELETE RESTRICT` so a referenced tag row cannot be dropped; unused tag rows are cleaned up explicitly by `TagRepository.pruneUnused()`.
  - **Composite primary keys** — `moment_timeline_memberships (moment_id, timeline_id)` and `moment_tags (moment_id, tag_canonical)` enforce membership/tag-link uniqueness. `media_attachments` has a UNIQUE(moment_id, sort_index) constraint so attachment ordering is deterministic and never collides.
  - **Tags** — canonical form (lowercased, whitespace-collapsed, trimmed) defines tag identity and is the primary key. The **first persisted display label wins**: insertion uses `INSERT ... ON CONFLICT(canonical) DO NOTHING`, so once a canonical row exists, attaching a later equivalent tag (differing only in case or whitespace, e.g. `Travel` → `travel` → `TRAVEL`) reuses the existing row without mutating its display label. This keeps historical UI presentation stable regardless of what a later moment typed. Editing a tag's display label is out of scope for the persistence layer and would require an explicit rename operation.
  - **Time** — persisted as `INTEGER` epoch milliseconds via the domain's `Instant(epochMilliseconds: Long)` value class. No timezone or calendar semantics live in persistence.
  - **Identity** — string PKs matching Phase 1A domain id types (`MomentId`, `TimelineId`, `MediaAttachmentId`). No database-generated numeric ids.
  - **Location** — coordinates and human-readable parts sit in the moments row itself (six nullable columns). Coordinate presence is validated by `ReliveLocation` on the way back out; the row-decoder returns `null` only when both coordinates and every readable field are absent.
  - **Indexes** — `moments(created_at)`, `moments(updated_at)`, `custom_timelines(created_at)`, per-side indexes on both membership tables and `moment_tags`, `media_attachments(moment_id)`. Sufficient for the timeline-scoped queries described in `PRODUCT_SPEC.md` §9; FTS is deliberately deferred until Phase 7's search UI can justify the added schema surface.
- **Consequences:** All persistence is on-device SQLite, no runtime SQL is hand-written outside SQLDelight, and every mapper is loud on corrupt values (`PersistenceMappingException`). Adding a table or query is a `.sq`-file edit; changing the schema requires a SQLDelight migration alongside `verifyMigrations = true`. Coroutines becomes a shared-module dependency, which is required for Phase 2+ anyway. Media binaries are still filesystem-owned — only opaque `MediaStorageRef` strings live in the database. Adopting SQLDelight is consistent with ADR-0002 (KMP + Compose MP) and preserves ADR-0004 (moments stored once; timelines reference moments; `All` is logical). Per ADR-0061 the Home surface's All moments feed reads a bounded, anchored, paged query rather than the unbounded `listAll()`, which remains available to non-UI callers such as export/backup; `Timeline.All` stays logical and unstored, and the root never hydrates the complete archive. See [`ARCHITECTURE.md`](ARCHITECTURE.md) §5.

---

## ADR-0014 — Platform date formatting via `expect/actual`, no new date dependency

- **Date:** 2026-08-20 · **Status:** Accepted
- **Context:** Phase 2 must show each moment's `createdAt` as an editorial eyebrow (e.g. `SEPTEMBER 28, 2023`) in the user's device time zone. The domain's `Instant` is a raw epoch-millisecond value class (`domain/time/ReliveTime.kt`); it deliberately carries no calendar or timezone semantics. Options considered: (a) add `kotlinx-datetime` to the shared module, (b) hand-roll Gregorian conversion in `commonMain`, (c) declare an `expect object EditorialDateFormatter` and implement it per platform. `kotlinx-datetime` is explicitly deferred for this phase, and hand-rolled Gregorian arithmetic is disallowed by the Phase 2 brief.
- **Decision:** Introduce `presentation.date.EditorialDateFormatter` as an `expect object` in `commonMain` with `fun format(instant: Instant): String`. The Android actual uses `java.text.SimpleDateFormat("MMMM d, yyyy", Locale.US)` bound to `TimeZone.getDefault()`, uppercased. The iOS actual uses `NSDateFormatter` with `en_US_POSIX`, `dateFormat = "MMMM d, yyyy"`, `timeZone = NSTimeZone.localTimeZone`, then `uppercaseStringWithLocale(en_US_POSIX)`. Both consume the domain `Instant` unchanged; no `kotlinx-datetime` dependency is added, and no Gregorian math lives in shared code. The formatter belongs to the presentation layer (it exists solely to build the string a UI reads), not to the domain, so the moment model stays platform-agnostic per ADR-0007.
- **Consequences:** Editorial dates are produced by well-tested platform calendar libraries and always render in the device's local time zone. Adding another formatted date variant is a one-line addition per platform actual. If a later phase legitimately needs cross-platform calendar arithmetic (e.g. Phase 6's 4-day rule already uses raw milliseconds, but future features like grouping by day/month may not), that phase records its own ADR — potentially introducing `kotlinx-datetime` at that point — rather than reversing this decision retroactively.

---

## ADR-0015 — Chronological timeline order applied at the presentation layer

- **Date:** 2026-08-20 · **Status:** Accepted · amended by ADR-0061 for the Home surface · superseded by ADR-0062 for custom timeline detail
- **Context:** ADR-0013's SQLDelight schema exposes `MomentRepository.observeAll()` as newest-first (`ORDER BY created_at DESC`), which is the canonical persistence contract. Phase 3 needs the All timeline to read like a WhatsApp chat: oldest moment at the top, newest saved moment at the bottom, with the inline composer pinned as the final timeline item immediately after the newest moment, and the initial viewport landed at the latest/bottom end so a newly kept moment appears just above the composer without a long animated scroll through history.
- **Decision:** Keep the repository ordering contract untouched. `AllTimelineViewModel` reverses the emitted list once (`moments.asReversed()`) before publishing it as `AllTimelineUiState.Loaded`, so presentation renders oldest → newest top-to-bottom. `AllTimelineScreen` renders the moments inside a `LazyColumn`, with the composer as the terminal `item("composer")` after the moment `items(...)`. A `rememberLazyListState()` tracks the item count: on the first non-empty snapshot the list `scrollToItem(moments.size)` (no animation) lands the user at the composer; on subsequent growth it `animateScrollToItem(moments.size)` so a freshly kept moment slides into view directly above the composer. SQLDelight ordering is never changed for UI reasons.
- **Consequences:** The persistence contract stays newest-first (cheap `LIMIT`/pagination for later phases still works), and any other consumer of `observeAll()` continues to receive that order. Presentation owns the visual chronology; changing the top/bottom orientation later is a single VM change, not a schema migration. See [`ARCHITECTURE.md`](ARCHITECTURE.md) §7 and [`PRODUCT_SPEC.md`](PRODUCT_SPEC.md) §5. Per ADR-0061 everything recorded above — oldest-first presentation order, the single `asReversed()` over a full `observeAll()` emission, the terminal composer item, the first-snapshot `scrollToItem`, and the growth `animateScrollToItem` — no longer describes the Home surface: its All moments feed is newest-first, bounded and paged, the app opens at the top of Home with no programmatic scroll to the composer or the newest Moment, and Keep Moment performs no app-initiated scroll of any kind. This ADR remains the record for custom timeline detail, whose ordering and entry behaviour are unchanged.

---

## ADR-0016 — Keyboard-aware inline composer (no detached floating composer)

- **Date:** 2026-08-20 · **Status:** Accepted
- **Context:** The composer is the final item in the timeline `LazyColumn`, not a separately positioned surface. When a composer input receives focus the IME appears and could either (a) obscure the active field, (b) force the app to hoist a detached floating composer above the keyboard, or (c) rely on hardcoded keyboard-height constants that break across devices and platforms. Options (b) and (c) both fragment the layout and diverge from the "timeline reads as one document" feel.
- **Decision:** The composer remains a normal timeline item. `AllTimelineScreen` applies `Modifier.windowInsetsPadding(WindowInsets.navigationBars.union(WindowInsets.ime))` at the timeline container, so the visible list is inset by whatever the IME actually reports. Compose's default focus-based auto-scroll then brings the focused field above the keyboard using the reported inset — no hardcoded heights, no manual `WindowInsets.ime.getBottom(...)` arithmetic, and no measurement of keyboard chrome anywhere in the codebase. Scrolling occurs only as needed to reveal the focused input; closing the keyboard collapses the IME inset back to zero and the timeline layout returns to its normal resting state.
- **Consequences:** One layout handles keyboard-open and keyboard-closed states; there is no second composer surface to keep in sync. Behavior is correct on any device the platform IME insets are correct on (Android IME animation callbacks, iOS keyboard notifications). If a future phase needs a truly overlayed composer (e.g. an expanded modal editor), it will be an additive surface, not a replacement of the inline one. See [`ARCHITECTURE.md`](ARCHITECTURE.md) §7. Per ADR-0061 the composer is still an ordinary item of the same scrolling list on the Home surface, but it sits at the chronological end of a newest-first feed — which renders at the feed's head — rather than as the list's final item; the inset and focus-scroll behaviour recorded here is unchanged.

---

## ADR-0017 — `SystemClock` and `UuidGenerator` as presentation-layer platform seams

- **Date:** 2026-08-20 · **Status:** Accepted
- **Context:** The composer needs a wall-clock reading (to stamp `createdAt`/`updatedAt` on a kept moment and to render the "now" eyebrow while composing) and a fresh identifier (to build `MomentId` for the persisted row). Both are inherently platform-provided. Options considered: (a) call `java.util.UUID`/`NSUUID` and `System.currentTimeMillis()`/`NSDate` directly from shared code — violates ADR-0007 by importing platform APIs into `commonMain`; (b) add `kotlinx-datetime` and a UUID multiplatform library to `shared` just for this — pulls two dependencies for one clock read and one string; (c) declare thin `expect object` seams that resolve to the platform primitive per target.
- **Decision:** Introduce two `expect object` seams in the presentation layer: `presentation.time.SystemClock : Clock` and `presentation.id.UuidGenerator : IdGenerator`, actualised in `androidMain` (JDK `System.currentTimeMillis()` / `java.util.UUID.randomUUID().toString()`) and `iosMain` (`NSDate.timeIntervalSince1970` / `NSUUID().UUIDString`). Both are exposed via the shared `ReliveAppContainer` (`clock`, `idGenerator`) and threaded down through `App` → `AllTimelineScreen` → `MomentComposerViewModel`. Tests substitute deterministic fakes at the container/VM boundary. The domain contracts (`domain.time.Clock`, `domain.id.IdGenerator`) stay platform-free and dependency-free.
- **Consequences:** Shared UI and shared tests never touch a platform API; deterministic composer tests do not need to mock time or generate UUIDs from real sources. No new shared-module dependency is added. Adding a third target (JVM, wasm, desktop) is a two-file `actual` addition, not a shared-code change. If a future phase legitimately needs richer calendar arithmetic or UUID semantics, it may introduce `kotlinx-datetime` / a UUID library then, superseding this ADR — but the composer itself will continue to depend only on the `Clock` / `IdGenerator` interfaces. Complements ADR-0007 and ADR-0014.

---

## ADR-0018 — Phase 4 media pipeline: optimized app-owned copies, non-destructive themes, platform-native compression

- **Date:** 2026-08-20 · **Status:** Accepted
- **Context:** Phase 4 introduces media capture (mic, camera) and library import into the composer, together with saved-timeline rendering. Relive is local-first and expected to accumulate years of moments on a personal device; keeping originals as source of truth would waste storage and expose Relive to arbitrary source formats and orientations. Camera/library flows also have to avoid touching the user's external files, and theme effects (Warm Journal / Monochrome Archive / Film Memory) must remain a rendering concern so a user changing themes later never rewrites their archive.
- **Decision:**
  1. **Optimized app-owned copies are the source of truth.** Every captured or imported blob is normalized/compressed by `MediaProcessor` into an app-owned file addressed by an opaque relative [`MediaStorageRef`](../shared/src/commonMain/kotlin/com/vaibhav/relive/domain/model/MediaAttachment.kt). Only the processed copy is persisted; no parallel "original" is kept. The user's external source file is never modified or removed.
  2. **Theme effects are non-destructive rendering only.** No sepia, monochrome, grain, or vintage color treatment is baked into the stored file. Themes apply via `ReliveTheme` at render time (see ADR-0010).
  3. **Temporary → processed → committed attachment lifecycle.** Media captured/imported is first written to a Relive-owned temp file; `MediaProcessor` produces a processed file with a fresh `MediaStorageRef`; that ref becomes a `DraftAttachment` in composer state. On successful Keep Moment, the file stays in place and the ref is written to the DB as-is (no second copy). Composer reset, per-attachment `×`, recording cancel, or processing failure all delete their draft files. A DB save failure preserves the draft files so retry does not recompress or duplicate.
  4. **Platform capabilities behind shared interfaces (ADR-0007).** New shared interfaces: [`MediaStore`](../shared/src/commonMain/kotlin/com/vaibhav/relive/platform/media/MediaStore.kt), [`MediaProcessor`](../shared/src/commonMain/kotlin/com/vaibhav/relive/platform/media/MediaProcessor.kt), [`AudioRecorder`](../shared/src/commonMain/kotlin/com/vaibhav/relive/platform/media/AudioRecorder.kt) (via `expect fun createAudioRecorder`), [`MediaPickerHandle`](../shared/src/commonMain/kotlin/com/vaibhav/relive/platform/media/MediaPicker.kt) (via `@Composable expect fun rememberMediaPickerHandle`), [`CameraCaptureSurface`](../shared/src/commonMain/kotlin/com/vaibhav/relive/platform/media/CameraCapture.kt) (`@Composable expect`), and playback composables (`RelivedImage/RelivedVideo/RelivedAudio`). Shared domain/presentation code never imports `android.*`, `AVFoundation`, `UIKit`, or file APIs.
  5. **Compression profiles.**
     - **Image** — orientation-corrected via EXIF, downscaled to ≤ 1920 px long edge (never upscaled), JPEG @ 82% quality. Android: `BitmapFactory` + `android.media.ExifInterface` + `Bitmap.compress`. iOS: `UIImage` (respects embedded orientation) + `UIImageJPEGRepresentation(0.82)`.
     - **Video** — H.264 video / AAC audio, ≤ 720p target, downscale-only. Android: **AndroidX Media3 Transformer 1.5.1** (`Presentation.createForHeight(720)`, `VIDEO_H264`, `AUDIO_AAC`). iOS: `AVAssetExportSession` with `AVAssetExportPreset1280x720`, MPEG4 output.
     - **Audio** — recorded as AAC/M4A at 64 kbps mono, 44.1 kHz. Android: `MediaRecorder` (MPEG_4 / AAC). iOS: `AVAudioRecorder` with `kAudioFormatMPEG4AAC`. Imported audio is copied through without re-encoding (already voice-appropriate on both platforms in practice).
  6. **Camera UX — single-camera Photo/Video switch.** Android: **AndroidX CameraX 1.4.2** (`camera-core`, `camera-camera2`, `camera-lifecycle`, `camera-view`, `camera-video`) with a shared `PreviewView`, `ImageCapture` + `VideoCapture(Recorder)` bound on the fly as the user toggles Photo/Video. iOS: `UIImagePickerController` with `sourceType = .camera` and `mediaTypes = ["public.image", "public.movie"]`, which is the native single-camera Photo/Video experience. System intents (`ACTION_IMAGE_CAPTURE`/`ACTION_VIDEO_CAPTURE`) were rejected because they cannot present the required in-camera mode switch.
  7. **Library UX — Photo / Video / Audio.** Tapping Library opens a small sheet with three choices, then launches the appropriate modern system picker: Android `PickVisualMedia` (image-only or video-only) for photo/video, `OpenDocument(["audio/*"])` for audio (no broad storage permission); iOS `PHPickerViewController` (no permission prompt) for image/video, `UIDocumentPickerViewController` for audio.
  8. **Playback — native only.** Android uses `MediaPlayer` on a `SurfaceView` (video) and `MediaPlayer` (audio). iOS uses `AVPlayer` + `AVPlayerLayer` (video) and `AVAudioPlayer` (audio). No Media3 UI / ExoPlayer UI is added; Media3 is present only as the Transformer backend.
  9. **Live recorder waveform is real.** `AudioRecorder.state: StateFlow<RecordingState>` exposes amplitude samples polled from the platform microphone (Android `MediaRecorder.getMaxAmplitude`, iOS `AVAudioRecorder.averagePowerForChannel` in dB → normalized). Samples are kept in a rolling window (96) so recomposition cost stays bounded. **Saved-audio waveform generation is deferred**: the timeline audio surface plays without a waveform rather than fabricating one.
  10. **Add Media actions are exactly Mic / Camera / Library.** The composer never surfaces top-level Photo or Video actions; those choices happen inside Library and Camera respectively.
- **New dependencies:**
  - `androidx.camera:camera-{core,camera2,lifecycle,view,video} = 1.4.2` — required for §Decision 6 (single-camera Photo/Video); system intents cannot deliver that UX.
  - `androidx.media3:media3-{transformer,common,effect,exoplayer} = 1.5.1` — required for §Decision 5's video normalization on Android; `exoplayer` is a transitive requirement of `transformer`.
  - `androidx.activity:activity-compose = 1.13.0` (added to shared `androidMain`; already used by `androidApp`) — for `rememberLauncherForActivityResult` bindings in the picker.
  - No other new media libraries. No ExoPlayer UI, no third-party transcoder, no iOS camera library.
- **Consequences:** Adding a new stored media kind means: a new `MediaType`, a `MediaStore.extensionFor` branch, a `MediaProcessor` branch, an `AudioRecorder`/`CameraCapture`/`MediaPicker` extension, and a `Relived*` composable. Themes can change freely without rewriting stored blobs. Original library files are never mutated (`copyToTmp` on Android, `PHPickerViewController` file-representation copy + `UIDocumentPickerViewController` security-scoped copy on iOS). Retrying a failed Keep Moment reuses the same processed files — no re-encoding. See [`ARCHITECTURE.md`](ARCHITECTURE.md) §6.2.

### Addendum — Camera polish (front/back, flash/torch, feedback)

- **Date:** 2026-08-21 · **Status:** Accepted, extends §Decision 6.
- **Rear camera is the default.** `CameraSelector.DEFAULT_BACK_CAMERA` on Android; iOS `UIImagePickerController` defaults to rear as well.
- **Front/back switching.** Android exposes an in-camera switch pill (rear ↔ front) that rebinds the same CameraX `Preview`/`ImageCapture`/`VideoCapture` use cases to the new `CameraSelector`; no camera screen re-open. Switch is hidden when `ProcessCameraProvider.hasCamera(DEFAULT_FRONT_CAMERA)` returns false, and disabled while a `VideoCapture` recording is in progress so rebinding never interrupts a capture. iOS gets the same behavior for free via `UIImagePickerController`'s native switch control.
- **Flash / torch — two states, one icon.** Both Photo and Video modes expose the same icon-only control with exactly two states: Off (outlined bolt) and On (filled bolt). No Auto. No text labels ("Flash On", "Torch On" and friends are all removed). Tap toggles Off ↔ On in both modes. Photo mode maps state to `imageCapture.flashMode ∈ {OFF, ON}`; Video mode maps state to `camera.cameraControl.enableTorch(true|false)` so the torch stays lit while recording. Default is Off. If the bound lens's `cameraInfo.hasFlashUnit()` is false (typical for the front lens), the icon is **shown muted (0.4 alpha) and inert** — taps do nothing — and any lingering On state is coerced to Off after rebind so the UI and hardware stay in sync. Screen-brightness "fake flash" is deliberately not implemented. On iOS Relive does not draw its own camera chrome — the native `UIImagePickerController` renders its own flash control (Off / On / Auto) and Relive does not override `picker.cameraFlashMode`. The scope of the two-state policy above is the Android in-camera surface where Relive is drawing the icon itself.
- **Camera switch.** Bottom-right control is a minimal two-arrow rotate glyph — no camera-body chrome, no text. In addition, **double-tap on the preview surface toggles front/back**, matching messaging-app cameras. Both entry points are disabled while a `VideoCapture` recording is in progress so a rebind never interrupts a capture, and both no-op if the device reports no front lens.
- **Feedback policy — platform-native sounds, no bundled assets.** All capture feedback comes from OS-provided sources; Relive bundles no manufacturer shutter/beep audio files, and none of these paths bypass silent-mode or regional camera restrictions (`MediaActionSound` is exactly the API the platform hands to camera apps for that purpose).
  - **Android — why not `MediaActionSound`.** The first iteration used `MediaActionSound.SHUTTER_CLICK` / `START_VIDEO_RECORDING` / `STOP_VIDEO_RECORDING`. Physical-device testing showed it is inaudible on Android 12+ and every OEM skin we tried (One UI, MIUI, ColorOS): `MediaActionSound` routes through `STREAM_SYSTEM_ENFORCED`, which those platforms gate to system-signed camera apps — `play()` returns cleanly, no audio reaches the speaker, and the media-volume slider does not affect it. Rejected.
  - **Android — implementation.** `android.media.ToneGenerator` on `AudioManager.STREAM_MUSIC` (volume 80/100). Uses the media-volume slider the user actually controls, works uniformly across OEMs, needs no bundled audio assets, and stays inside the "OS-provided" bucket. Tone codes: photo → `TONE_PROP_ACK` (90 ms), video start → `TONE_PROP_PROMPT` (140 ms), video stop → `TONE_PROP_BEEP2` (160 ms). Each `startTone(...)` call is wrapped in a try/catch since the `ToneGenerator` constructor is documented to throw `RuntimeException` when no `AudioTrack` is free; the whole feedback path degrades to haptic-only in that case rather than crashing camera.
  - **Android — photo capture.** Tone + `VibrationEffect.EFFECT_CLICK` haptic fire inside CameraX's `onImageSaved` callback (real success only, never on tap-with-failure). Photo capture does not open the mic, so no audio-contamination risk.
  - **Android — video start (mic-safe ordering).** Tap on shutter plays the start tone and haptic, then a `rememberCoroutineScope` coroutine `delay(START_TONE_MS + START_TONE_GUARD_MS)` (140 + 80 ms) **before** calling `videoCapture.output.prepareRecording(...).start(...)`. CameraX opens the `AudioSource` inside `.start(...)`, so by the time the mic is live the speaker is silent and the tone cannot bleed into the video's audio track. The perceptible delay before recording begins is ~220 ms — acceptable UX cost for a clean recording. The trailing `Video Start` event fires from CameraX's `VideoRecordEvent.Start` as before but no longer plays audio (it would land inside the recording window).
  - **Android — video stop.** `VideoRecordEvent.Finalize (no error)` fires **after** CameraX has released the microphone; only then do we play the stop tone and haptic, so the stop tone cannot enter the recorded track.
  - **iOS.** `UIImagePickerController` provides the native shutter click, recording-start tone, and recording-stop tone itself. Relive does not layer additional sounds on top; iOS's own routing already excludes those tones from the recorded video audio track.
- **Lifecycle.** On camera dispose, back, cancel, or a failed video record, `boundCamera.cameraControl.enableTorch(false)` runs before `provider.unbindAll()`, and any in-flight temp file is deleted. `ToneGenerator.release()` runs on dispose (guarded in try/catch) so the audio resource never leaks past the screen.
- **Layout.** Bottom safe-drawing bar carries Cancel | shutter | camera-switch; flash icon sits top-left. No top `×` (would hide behind cutouts). All controls use `WindowInsets.safeDrawing` — no hardcoded status/nav bar heights. (Superseded by the WhatsApp-style polish addendum below; the polish layout drops the Cancel pill entirely — system Back is the sole cancel path — and reorganizes the bottom controls.)
- **No new dependencies.** All of the above uses APIs already available via the CameraX 1.4.2 stack and platform Android SDK.

### Addendum — WhatsApp-style camera polish

- **Date:** 2026-08-21 · **Status:** Accepted, extends the addendum above.
- **Layout.** The Android camera surface now follows a WhatsApp-style vertical hierarchy: full-screen preview at the back, a small flash icon in the upper-left preview corner, and a black bottom control bar that hosts (top → bottom) zoom presets, the main control row (Gallery / Filter / Shutter / Switch), and the Photo/Video mode selector. All chrome insets from `WindowInsets.safeDrawing` — no hardcoded status/nav-bar heights, no Relive cream/brown styling inside the camera.
- **Capture button is the fixed geometric center.** The main control row is a `Box(fillMaxWidth)` with the shutter placed at `Alignment.Center`; Gallery+Filter pin to `Alignment.CenterStart` and Switch pins to `Alignment.CenterEnd`. A `Row(SpaceBetween)` would let the shutter drift whenever left/right widths differ — that path is deliberately not used. The zoom preset row above the shutter and the Photo/Video selector below are each independently centered on the same horizontal axis.
- **Gallery + Retro filter controls.** The Gallery control is a dark circular button with a stacked-photos glyph; it dismisses the camera and hands off to the composer's existing Library sheet (Photo / Video / Audio) via a new `onOpenGallery` callback on `expect fun CameraCaptureSurface`. The iOS actual ignores the callback — `UIImagePickerController` already surfaces the on-device library from its own controls. Retro filters are represented by a dark circular sparkle glyph that is currently inert; the full retro-filter processing system is reserved for a future phase and no filter dependencies are added.
- **Cancel.** The explicit Cancel pill is removed. System Back and camera dispose remain the only cancel paths and preserve the existing behavior (stop any active recording, delete any in-flight temp file, disable torch, unbind CameraX, return to composer without leaving Relive).
- **Zoom (Pixel-style dynamic presets + live ruler).** The bottom slot holds one of two visual states in the same fixed-height container so the shutter row never shifts. Normal state renders three zoom slots {Low, Mid, High} via `zoomSlotsFor(min, max)`, filtered by the *actual* CameraX `zoomState` range of the currently bound lens — 0.5× (Low) only when the lens's real `minZoomRatio ≤ 0.5`, 2× (High) only when `maxZoomRatio ≥ 2`, no upscaled or faked digital 0.5×. `activeZoomSlot(ratio, slots)` chooses the highlighted slot; that slot displays the exact current ratio via `formatZoomLabel` (e.g. `0.7×`, `1.4×`, `3.5×`) while inactive slots show their anchor label. During a two-finger pinch the slot swaps to a live ruler (`ZoomRulerRow` — log-scaled tick strip with a current-value pill above); on release the ruler lingers ~850 ms, then presets return with the intermediate ratio still displayed in the appropriate slot. Preset taps clamp to the reported CameraX range via `clampZoomRatio(...)`. Pinch uses `awaitEachGesture` (not `detectTransformGestures`) so gesture start/end is observable and the ruler can swap cleanly.
- **Legacy zoom preset helpers.** `availableZoomPresets(min, max)`, `activePresetIndex(ratio, presets)`, `ZoomPreset` remain in `CameraControls.kt` for the earlier tests and for any future non-Pixel-style surface; the Android UI now uses `ZoomSlot`/`zoomSlotsFor`/`zoomSlotLabel`.
- **Zoom lifecycle across rebinds.** Default zoom is 1× when the lens supports it, otherwise `defaultZoomRatio(...)` falls back to the reported minimum. On a Photo↔Video rebind the current ratio is preserved when the new binding still reports the same range; on a Back↔Front rebind (where the front lens typically reports a 1×-only range) the ratio is coerced to the lens default. Pinch runs in its own `pointerInput` so it composes with the double-tap detector rather than swallowing it, and it does not trigger capture.
- **Camera switch.** The dedicated switch control is a minimal loop-arrow icon in a 48 dp dark circular surface, no text, hidden when the device reports no front lens, and disabled while a video recording is in progress. Double-tap on the preview surface remains an equivalent switch shortcut (single tap is a no-op — it never triggers capture; both entry points ignore taps while recording).
- **Flash.** Position, icon set, and two-state Off/On policy are unchanged from the prior addendum. The control now sits inside the upper-left of the preview and is not part of the bottom control bar, so it cannot disturb the shutter's geometric centering.
- **Photo/Video selector.** Bottom pill shows only `Video Photo` (in that visual order); Photo is selected by default; the selected chip fills a dark rounded surface with white text, unselected chips are transparent with white text. Video Note is intentionally absent. The selector sits above `WindowInsets.safeDrawing` so it never overlaps gesture or three-button navigation.
- **Feedback policy.** Unchanged from the prior addendum — `ToneGenerator` on `STREAM_MUSIC` for photo/video-start/video-stop, ordered so the start tone completes before CameraX opens the mic. Photo captures also guard against a re-entrant tap while a capture is in flight so a double-tap never enqueues two shutter events.
- **No new dependencies.**

### Addendum — Capture filter removal

- **Date:** 2026-08-23 · **Status:** Accepted, supersedes the Retro filter portion of the WhatsApp-style camera polish addendum.
- **Decision:** Remove the inert Retro filter control from the Android photo and video capture UI. The main control row now contains Gallery, the geometrically centered shutter, and camera switch only.
- **Consequences:** No filter icon, label, semantics, callback, or touch target remains. Photo and video capture behavior, camera controls, review flow, and media processing are unchanged. No filter-processing architecture is introduced or removed.

---

## ADR-0019 — Timeline media uses an adaptive visual collage

- **Date:** 2026-08-21 · **Status:** Accepted
- **Context:** The timeline is an editorial memory journal, not a chat feed or generic attachment list. Media is a first-class part of a Moment and should be visually prominent. The existing warm Relive identity remains authoritative: cream journal canvas, timeline rail and dots, Playfair editorial titles, Inter body text, warm brown accent, chronological timeline, no enclosing card around an entire Moment. ADR-0018 §9 deferred saved-audio waveform generation. Phase 4's carousel model (PRODUCT_SPEC §5, DESIGN_SYSTEM §14, ROADMAP Phase 4) needs to be revisited: a horizontal carousel fragments the visual hierarchy, hides attachments behind swipe discovery, and does not express the editorial density the product demands. WhatsApp is a behavioral reference for efficient multi-media presentation only — Relive must not copy WhatsApp's chat bubbles, colors, typography, or chat visual language.
- **Decision:**

  **1. Adaptive collage replaces the carousel.**
  Multiple attachments in the timeline use an inline adaptive collage instead of a horizontal carousel/pager. This supersedes the carousel model described in PRODUCT_SPEC §5, DESIGN_SYSTEM §14, ROADMAP Phase 4, ARCHITECTURE §5, and TESTING §Phase 4.

  Collage rules by attachment count:

  | Count | Layout |
  |-------|--------|
  | 1 | Large responsive media tile using most of the available Moment content width. Single images preserve their natural aspect ratio as much as practical; portrait images remain meaningfully portrait rather than being squeezed into a short landscape viewport. |
  | 2 | Two equal visual tiles side-by-side. |
  | 3 | Asymmetric composition: one dominant tile + two smaller vertically stacked tiles. |
  | 4 | 2×2 grid. |
  | 5+ | First four tiles rendered inline; the fourth tile receives a translucent `+N` overlay representing the remaining attachment count. |

  Attachment order is deterministic and follows the Moment attachment ordering contract (ADR-0013 `UNIQUE(moment_id, sort_index)`). Grid tiles may use controlled cropping to maintain collage geometry, but media must never be stretched or distorted.

  **2. All media types participate in the collage.**
  The adaptive collage is not image/video-only. All three existing media types — Image, Video, Audio — are first-class visual timeline tiles. A Moment may therefore contain mixed layouts such as Photo|Audio or Photo|Video / Audio|Photo. Audio must not be moved into a separate generic player underneath the collage.

  **3. Video presentation in the timeline.**
  Video tiles occupy the same grid system as images. Each video tile shows a representative thumbnail/frame with a subtle Play affordance. No autoplay. No persistent active video player merely because a tile is visible. Actual consumption occurs through the media viewer (§7 below).

  **4. Audio visual identity in the timeline.**
  Audio is represented as a visual media tile, not a traditional horizontal audio player, equalizer bars, voice-message bubble, generic Material audio card, or random animated bars.

  *Audio canvas:* The tile is a large black/near-black media canvas. Inside that canvas is a compact, centered waveform visualization region. The waveform must not stretch across most of the black tile — the black negative space is intentional. The waveform region occupies approximately 45–60% of the available tile width, with generous space on the left and right and comfortable vertical space around it. Visual hierarchy: large black media canvas → compact centered sound visualization → minimal playback affordance.

  *Real waveform requirement:* The waveform represents the actual audio signal, not random data. Silence → nearly flat; quiet audio → small amplitude; normal speech/music → moderate amplitude; loud audio → larger peaks; sharp events → corresponding real peaks. The waveform communicates the structure of the actual saved recording. This is an architectural/design requirement, not decoration.

  *Waveform visual form (revised 2026-08-21):* The earlier "continuous waveform/sound trace" form is superseded. The waveform is a compact row of white vertical rounded-capsule amplitude segments on the black canvas, symmetric around the horizontal midline, uniform segment width, amplitude driving height only. Segments are not decorative equalizer bars — each segment's height comes from a real bucket of the extracted amplitude envelope, so silence renders as a tiny centered dot, quiet audio as short capsules, and loud peaks as tall capsules. Approximately 9–17 visible segments depending on tile width (smaller collage tiles get fewer segments; the same rounded-capsule identity is preserved). No random amplitude, no per-segment bounce animation, no bar-width variation.

  *Waveform paused state:* Real waveform window visible, segments stationary, minimal Play affordance, subtle duration indicator. Pausing does not reset the window.

  *Waveform playing state:* The visible segments represent a bounded window into the real envelope centered on the current playback position. As playback advances, past segments exit toward the left and future segments enter from the right — the shape flows horizontally through the viewport. Segment heights remain determined by real envelope data at every frame; there is no fabricated animation. Near the head and tail of the recording the window clamps to real data so playback still finishes at the actual end of the envelope.

  *Waveform processing principle:* Saved audio is analyzed to derive a lightweight normalized/downsampled amplitude envelope: audio → decode/sample → amplitude envelope → normalize → downsample → reusable waveform representation. Do not repeatedly decode the entire audio file during Compose recomposition. Waveform information should be reusable/cached. This ADR does not introduce a database/schema change; if implementation later determines persistent waveform metadata is necessary, that requires a separate explicit architecture decision.

  **5. Full-screen media viewer.**
  Timeline = browsing memories. Media viewer = consuming media. Tapping a timeline media tile opens a dedicated dark/full-screen media viewer at the exact attachment that was tapped. If a Moment has multiple attachments, the viewer allows horizontal navigation through all attachments of that Moment. For 5+ attachments, tapping the `+N` tile opens the viewer and all hidden attachments remain accessible. No carousel in the timeline; swiping between attachments is allowed only in the dedicated viewer.

  **6. Image viewer.**
  Dark/black background. Image initially fitted appropriately, preserving aspect ratio/orientation. Pinch-to-zoom. Pan while zoomed. Double-tap zoom where practical. Android Back returns to the timeline without losing the user's scroll position. No editing controls.

  **7. Video viewer.**
  Dark viewing surface. Correct aspect ratio/orientation. Play/Pause. Playback progress/seeking. Audio. No timeline autoplay. Reuse existing media playback architecture where practical (ADR-0018 §8).

  **8. Audio viewer.**
  Full-screen audio preserves the same visual identity as the timeline audio tile: black viewing surface → larger but still intentionally bounded real waveform visualization → minimal Play/Pause → playback progress/duration. The real waveform remains synchronized with playback. It must not become a generic audio-player screen.

  **9. Performance constraints.**
  Timeline scrolling must remain smooth. Avoid: decoding original full-resolution images unnecessarily; constructing video players for every video tile; repeatedly decoding audio to regenerate waveforms; animating off-screen waveform tiles; eagerly loading hidden 5+ attachments; unnecessary recomposition caused by playback progress. Prefer: existing optimized Relive media copies (ADR-0018 §1); thumbnails/representative video frames; lazy media loading; cached/reusable waveform envelope data; playback resources created only when needed; lifecycle-aware cleanup. If practical, only one audio/video playback session should be active at a time.

  **10. Material 3 usage.**
  Material 3 should be used wherever it provides appropriate behavior/accessibility primitives: Play/Pause, navigation/back, interaction states, touch targets, semantics, standard controls. But Material 3 must not override Relive's visual identity (ADR-0003, ADR-0010). Do not introduce large icon dependencies merely for a few standard glyphs when lightweight/local vectors are sufficient.

  **11. Accessibility.**
  Minimum appropriate touch targets (DESIGN_SYSTEM §18). Meaningful content descriptions. Play/Pause state exposed semantically. Media type identifiable. `+N` exposes that more attachments are available. Interactions must not rely solely on color.

  **12. Explicit non-goals.**
  This decision does not change: timeline chronological ordering (ADR-0015), timeline rail/dots, composer, camera (ADR-0018 addenda), capture behavior, media compression policy (ADR-0018 §5), media persistence/storage semantics (ADR-0018 §1), favorite behavior, search (ADR-0006), settings, location (ADR-0008), or custom timelines.

- **Consequences:** The carousel model from PRODUCT_SPEC §5, DESIGN_SYSTEM §14, ROADMAP Phase 4, ARCHITECTURE §5, and TESTING is superseded for timeline media presentation. Those documents are updated to reference the adaptive collage. The `media.ratio.square`, `media.carousel.peek` tokens in DESIGN_SYSTEM §14 are superseded; collage geometry is defined by this ADR's layout rules. ADR-0018 §9's deferral of saved-audio waveform generation is superseded — this ADR commits to real waveform visualization as a design requirement (the specific waveform processing implementation remains a Phase 5+ task). Horizontal swiping between attachments is available only inside the full-screen media viewer, never in the timeline itself. §9's media rules are per rendered row; per ADR-0061 the Home surface additionally bounds how many Moment rows exist at all — its All moments feed observes a paged window and never the complete archive.

---

## ADR-0020 — Editorial time formatter via `expect/actual`, extending date eyebrow to `DATE • TIME`

- **Date:** 2026-08-22 · **Status:** Accepted
- **Context:** Phase 2 shipped with a date-only eyebrow (`SEPTEMBER 28, 2023`). Timeline and composer both benefit from showing time-of-day alongside the date. ADR-0014 established the `expect/actual` pattern for `EditorialDateFormatter`; the same pattern applies to time.
- **Decision:** Introduce `presentation.date.EditorialTimeFormatter` as an `expect object` with `fun format(instant: Instant): String`. Android actual uses `SimpleDateFormat("h:mm a", Locale.US)` with `TimeZone.getDefault()`; iOS actual uses `NSDateFormatter` with `dateFormat = "h:mm a"` and `localTimeZone`. The saved-moment eyebrow renders as `DATE • TIME` (e.g. `AUGUST 22, 2026 • 10:48 AM`) on a single row. Both formatters consume the same immutable `createdAt` in the device's local time zone. The `type.eyebrow` token description in `DESIGN_SYSTEM.md` is updated to reflect `DATE • TIME`.
- **Consequences:** Complements ADR-0014. No new dependencies. Consistent device-local time display across both platforms. The composer shows time in a separate label below the date using the same formatter.

---

## ADR-0021 — Inline-vs-fullscreen single-video playback rule

- **Date:** 2026-08-22 · **Status:** Accepted
- **Context:** Single-video Moments use adaptive natural sizing (ADR-0019 §1). Some videos fit within timeline bounds without being constrained; others must be scaled down. Inline playback is desirable when the video is already at comfortable viewing size, but loses value when the video was squeezed to fit constraints — the user would benefit more from immediate full-screen viewing.
- **Decision:** The adaptive sizing pass produces a `wasConstrained` flag. If the single video was **not** constrained (natural size fits within timeline max bounds): Play starts **inline** in the same adaptive bounds; body tap opens the full-screen viewer. If the video **was** constrained: both Play and body tap open the **full-screen viewer** directly; no inline player is created. Multi-media collage video tiles always navigate to the gallery/viewer. The flag is computed once per tile from `computeAdaptiveMediaPreview()` and does not change during scroll.
- **Consequences:** Small/naturally-fitting videos get quick inline playback. Large videos skip the compromised inline view and go straight to full-screen. No unnecessary player creation for constrained tiles. Complements ADR-0019 §3 and §9.

---

## ADR-0022 — Multi-media gallery as intermediate navigation surface

- **Date:** 2026-08-22 · **Status:** Accepted
- **Context:** ADR-0019 §5 specifies a full-screen viewer for media consumption. For Moments with 2+ attachments, opening the viewer directly from a collage tile would skip the opportunity to see all attachments at once. For single-attachment Moments, the gallery adds no value — the viewer alone suffices.
- **Decision:** Navigation hierarchy by attachment count: **1 attachment** — timeline tap opens the full-screen viewer directly. **2+ attachments** — timeline collage tap (including `+N`) opens a dedicated `MomentMediaGallery` first, showing all attachments for that Moment in a vertically scrollable adaptive grid on a dark surface. Tapping a gallery item opens the full-screen viewer at that index. Viewer Back returns to the gallery (scroll position preserved). Gallery Back returns to the timeline (scroll position preserved). State is tracked via `TimelineMediaNavState`, which holds optional gallery and viewer overlays.
- **Consequences:** Gallery provides attachment overview for multi-media Moments without cluttering the single-attachment path. Scroll positions are preserved at every level. Gallery is a presentation concern; it does not change persistence or domain models. Complements ADR-0019 §5.

---

## ADR-0023 — Media presentation caching and single-owner playback

- **Date:** 2026-08-22 · **Status:** Accepted
- **Context:** Media-heavy timelines with images, video thumbnails, and audio waveforms can stall the UI thread if each tile eagerly decodes media, creates players, or repeatedly extracts waveforms. ADR-0019 §9 requires smooth timeline scrolling and lifecycle-aware cleanup.
- **Decision:**
  1. **`MediaPresentationCache`** — bounded in-memory cache keyed by `MediaStorageRef` for video thumbnails (`NaturalSizePx` + bitmap), image natural dimensions, and audio waveform envelopes. All cache hits are off-main-thread; cache misses trigger background extraction. Cache is bounded and entries evict under memory pressure.
  2. **Lazy player creation** — passive audio/video tiles in the timeline do not create `MediaPlayer`/`AVPlayer` instances. Players are instantiated only when the user taps Play.
  3. **`ActivePlayback` single owner** — at most one audio or video playback session is active at any time. Starting a new playback stops the prior one. Navigation (opening gallery, viewer, or returning to timeline) stops the active playback. Compose disposal releases the player.
  4. **Scoped recomposition** — playback progress updates are scoped to the playing tile; the rest of the `LazyColumn` does not recompose for playback ticks. Stable `MomentId` keys prevent unnecessary item recomposition during scroll.
- **Consequences:** Timeline scrolling performance is protected. Waveform data is reusable without re-decoding audio. Only one set of playback resources is live at any time. Off-screen tiles release resources on disposal. Complements ADR-0019 §9 and ADR-0018 §8.

---

## ADR-0024 — Timeline Home is the navigation root

- **Date:** 2026-08-22 · **Status:** Partially superseded by ADR-0061
- **Context:** Relive supports All plus custom timelines, but the former in-detail selector was the only entry point. The product needs a memory-oriented collection layer without redesigning the approved Timeline detail UI.
- **Decision:** Timeline Home is the root screen. It renders a bounded, reactive summary for All and every custom timeline, then opens the existing `TimelineScreen` scoped to the selected timeline. Timeline detail retains its selector and receives a minimal Back affordance when entered from Home. Summary previews select at most four image/video attachments ordered by Moment `createdAt` descending then attachment `sortIndex` ascending; All remains a logical query.
- **Consequences:** The app has a clear Home → detail hierarchy while Moment ownership and membership remain unchanged. The read projection avoids per-card full-Moment hydration and uses existing thumbnail/cache infrastructure. Home remains the single creation entry point for this phase; no profile, settings, menu, or bottom navigation is introduced. Partially superseded by ADR-0061: the app's landing root is the Home surface, All moments is a scroll region of that surface rather than a summary card that opens a scoped `TimelineScreen`, and there is no Home → detail hop or Back affordance for All. Timeline Home itself is retained as the root of the Timelines destination, where it continues to list custom timelines and open their existing scoped detail screens unchanged.

---

## ADR-0025 — Rediscover is a bounded local read model and second top-level destination

- **Date:** 2026-08-23 · **Status:** Accepted · destination portion superseded by ADR-0061
- **Context:** Timeline Home was the app-wide root under ADR-0024. Relive now needs a calm resurfacing experience without turning it into another chronological timeline, backend recommendation system, or a full archive hydration path.
- **Decision:** Timelines and Rediscover are the two top-level destinations until Search and You are implemented. Timeline Home remains the root inside Timelines. Rediscover is a dedicated SQLDelight-backed read model: On This Day returns at most 20 current-device-local prior-year matches; From Your Past returns four deterministic daily selections at least 90 days old; Places and Tags rank existing persisted usage. No new table, cloud state, analytics, AI, or recommendation backend is introduced. Media presentation reuses passive cached primitives and opens the existing viewer on demand.
- **Consequences:** This supersedes ADR-0024 only where it names Timeline Home as the app-wide navigation root. Rediscover never calls `MomentRepository.observeAll()` and batch-hydrates selected tags/attachments to avoid N+1 reads. A user timezone change may change anniversary eligibility, matching existing device-local editorial date formatting. ADR-0061 supersedes this ADR only for Rediscover being a second top-level destination and for Timeline Home being the root inside Timelines: the app's single root is the Home surface, and Rediscover's collections are a horizontally scrollable row inside it. The bounded read-model requirement recorded here is retained and now extends to the whole Home surface — no part of it calls an unbounded `MomentRepository.observeAll()`, and the All moments feed reads a bounded, paged window.

---

## ADR-0026 — Rediscover starts with Favorites; resurfacing sections are deferred

- **Date:** 2026-08-23 · **Status:** Superseded by ADR-0028
- **Context:** The approved Rediscover read model is useful future infrastructure, but its On This Day, From Your Past, Places, Tags, and empty-state presentations are not the current product root.
- **Decision:** Rediscover renders only the Relive app bar and a persisted Favorites system-collection card above the existing two-item bottom navigation. The existing SQLDelight Rediscover repository, queries, calendar seam, and tests remain preserved for a future presentation phase, but the app does not construct `RediscoverViewModel` or collect its flows while the sections are absent. Favorites alone observes persisted Moments to calculate its real count.
- **Consequences:** The visible-section portion of ADR-0025 is superseded. No Rediscover title, subtitle, resurfacing section, or related empty state remains in the active root. The later implementation can reuse the preserved local read model without reintroducing persistence changes. This entry is historical in a second way: per ADR-0061 Rediscover has no screen, app bar, or bottom navigation of its own — it is a collection row inside the Home surface.

---

## ADR-0027 — Favorites is a reactive, read-only Rediscover system collection

- **Date:** 2026-08-23 · **Status:** Superseded by ADR-0028
- **Context:** Favorites is the first active Rediscover feature and must remain a direct expression of Moment favorite state rather than another user-managed timeline.
- **Decision:** SQLDelight observes the favorite count, favorited Moment scope, and a bounded four-item visual cover directly from `moments` and `media_attachments`. The card has a heart icon plus Moment count, never a visible Favorites label. Its nested detail reuses Timeline presentation through `TimelineMode.ReadOnlySystemCollection`, which removes all write affordances and rejects mutation requests at the presentation boundary while retaining media viewing/playback.
- **Consequences:** A favorite change anywhere updates Rediscover without duplicate persistence. Normal timelines retain full mutation capability. The deferred Rediscover overview projections remain uncollected at the active root. The former Android debug QA dataset was later retired by ADR-0074.

---

## ADR-0028 — Rediscover Favorites is a bounded per-Moment shelf

- **Date:** 2026-08-23 · **Status:** Accepted
- **Context:** The original Favorites collection card hid individual memories behind a single aggregate surface. Rediscover should instead provide a compact, editorial shelf that lets a person enter the read-only Favorites timeline at a specific memory without changing favorite persistence or collection semantics.
- **Decision:** Rediscover renders a `FAVOURITES` heading, followed by a horizontally swipeable row of at most ten individual favorited-Moment cards and a text `Show all` action. Each card uses the full Favorites timeline's chronological ordering, first ordered attachment as its lead visual, and an attachment-count indicator when needed. Tapping a card opens the read-only Favorites timeline positioned at that Moment; `Show all` opens the same timeline without a selected Moment. With no favorites, Rediscover shows the approved two-line empty state and no row or action. The shelf is fed by a dedicated bounded SQLDelight projection that batch-loads its attachments, rather than hydrating the complete collection or issuing per-Moment attachment reads.
- **Consequences:** The single aggregate Favorites card and its visual-cover query are superseded. Favorite state remains the only source of truth; no memberships, mutation controls, passive players, or full-resolution image decoding are added. The Favorites detail remains read-only and Back restores the Rediscover root's saved scroll state. Per ADR-0061 there is no Rediscover root: Favourites is one card in the Home surface's Rediscover row, under the single `Relive your memories` heading, and Back from the read-only Favourites collection restores the Home surface at its saved scroll offset without forcing it back to the top state. The bounded per-Moment projection recorded here is unchanged.

---

## ADR-0029 — Compact Favorite shelf cards share a fixed visual region

- **Date:** 2026-08-23 · **Status:** Accepted
- **Context:** ADR-0028 specified attachment-led compact cards, including no empty media frame for text-only Moments. The horizontal shelf now needs stable card geometry so image/video, text-only, and audio-only Favorites swipe as one visual collection.
- **Decision:** In the compact Rediscover Favorites shelf only, every card has the same fixed card and visual-region height. Image/video cards render the first ordered attachment. Text-only and audio-only cards render the existing theme-aware card surface as an empty visual placeholder; they contain no fake media, icon, or illustration. The lower region retains Moment information and a non-interactive favorite-status heart. Normal Timeline presentation and the read-only Favorites timeline are unchanged.
- **Consequences:** This supersedes ADR-0028 only for compact text-only/audio-only shelf media treatment. It does not introduce new data, behavior, media handling, or favorite mutation paths. Per ADR-0061 the Home surface carries one horizontally scrollable row of collection cards — Favourites, On This Day, From Your Past, All Photos — rather than per-Moment shelves, so this card geometry no longer describes anything on the Home surface.

---

## ADR-0030 — On This Day is a bounded, read-only calendar anniversary collection

- **Date:** 2026-08-23 · **Status:** Accepted
- **Context:** Rediscover now needs its next shipped collection without changing the established local-first archive or the existing Favorites behavior.
- **Decision:** On This Day queries a bounded, attachment-batched local projection for Moments whose current-device-local month/day matches today in a previous local calendar year. The current year is excluded; February 29 remains exact. Root cards are horizontally swipeable featured cards with stable media/text/audio dimensions and exact calendar-year labels. Tapping opens the existing read-only system-collection timeline at the selected Moment.
- **Consequences:** No membership, duplicate Moment, mutation controls, autoplay, or full-archive hydration is introduced. Places and Tags remain deferred; From Your Past is subsequently activated by ADR-0031. Per ADR-0061 there are no Rediscover root cards: On This Day is the second card in the Home surface's Rediscover row, and the featured per-Moment presentation described here belongs to the opened read-only collection.

---

## ADR-0031 — From Your Past is a ten-Moment deterministic daily system collection

- **Date:** 2026-08-23 · **Status:** Accepted
- **Context:** ADR-0025 described an earlier deferred From Your Past projection with a limit of four Moments. Rediscover now needs that local resurfacing collection while preserving the approved Favorites, On This Day, navigation, and read-only system-collection architecture.
- **Decision:** From Your Past renders below On This Day and returns up to ten distinct Moments from a bounded SQL-backed query. Eligible Moments are at least 90 days old, are not future timestamps, and are not currently eligible for On This Day. A local-date-derived seed makes selection and its order stable for a local day and rotates it on the next day; no recommendations are persisted. The shelf reuses the compact Favorites card geometry and media treatment but has no heart. A card opens the exact daily selection in `TimelineMode.ReadOnlySystemCollection`, positioned at that Moment and retaining shelf order.
- **Consequences:** This supersedes ADR-0025 only for From Your Past's limit, which changes from four to ten, and its deferred presentation status. No backend, membership, mutation path, autoplay, or archive hydration in Compose is introduced. Places and Tags remain deferred. Per ADR-0061 From Your Past is the third card in the Home surface's Rediscover row, following On This Day in that row's horizontal order rather than rendering as a section below it; its bounded daily selection is unchanged.

---

## ADR-0032 — Profile is an auxiliary destination with truthful installation dating

- **Date:** 2026-08-23 · **Status:** Accepted
- **Context:** Profile needs a joining date and archive statistics without changing the two top-level destinations or deriving installation identity from Moment activity.
- **Decision:** Timeline Home opens Profile as an auxiliary destination with a visible Back affordance; Profile is not placed in bottom navigation and Timeline Home retains its list state beneath it. A singleton `profile_metadata` record is inserted exactly once by the fresh SQLDelight schema. The migration creates the table without a row, so existing installations retain an unknown joining date and Profile hides its Since line. Profile statistics are reactive SQL count/distinct projections: all Moments, persisted custom timelines only, and distinct readable persisted location tuples.
- **Consequences:** No migration-time, oldest-Moment, or current-launch date is fabricated. Profile does not hydrate the complete Moment archive just to calculate its statistics. Settings rows remain presentation-only until their real destinations are implemented. Per ADR-0061 the top-level destination set is Home / Timelines / Search rather than Timelines/Rediscover, and it is the Home surface that opens Profile as an auxiliary destination, retaining its scroll and list state beneath it; Profile itself stays auxiliary and outside top-level navigation.

---

## ADR-0033 — Timeline navigation is home-scoped and All lives in Rediscover

- **Date:** 2026-08-23 · **Status:** Partially superseded by ADR-0061
- **Context:** The original Timeline Home and in-detail selector made two locations responsible for timeline switching, while the collection root now needs All beside the existing Rediscover collections.
- **Decision:** Timeline Home lists custom timelines only. Creating a custom timeline persists it, orders it newest-created-first (with id as the deterministic timestamp-tie break), and opens that exact editable timeline immediately. Timeline detail contains no selector or create-timeline control; Back returns to the prior root. The existing All summary card/read projection is rendered first in Rediscover and opens editable All, followed by Favourites, On This Day, and From Your Past. Custom cards expose their persisted creation date. The timeline rail ends at the composer plus marker, and Keep Moment is a Relive-colored Material 3 primary button.
- **Consequences:** This supersedes the Timeline Home placement portions of ADR-0021 and any TimelineScreen-owned switching behavior. All remains logical and editable; system Rediscover collections remain read-only. No Moment persistence or membership behavior changes. Partially superseded by ADR-0061: there is no All summary card and no Rediscover root to render it on — the editable All moments timeline is part of the Home surface, directly beneath the `All moments` heading, and is reached by scrolling, never by a card tap. This ADR's rule that Timeline Home lists custom timelines only is **retained**, and Timeline Home remains the root of the Timelines destination. The Home surface's Rediscover row contains, in order, Favourites, On This Day, From Your Past, and All Photos. Custom timeline detail keeps its existing entry and Back behaviour, and All has no detail screen: Back from its inline composer collapses the composer and leaves the user in focused All moments.

## ADR-0034 — Global, read-only Search v1

- **Date:** 2026-08-23 · **Status:** Accepted
- **Context:** The previous Phase 8 plan placed a complex, timeline-scoped search transform in every timeline header, including filters, Tags, Places, and suggestions. That conflicts with the need for a focused archive search and duplicates timeline-specific UI.
- **Decision:** Search is the third top-level destination alongside Timelines and Rediscover. It opens a dedicated, autofocus screen and queries every local Moment by case-insensitive SQL `LIKE` over title and content. Results use All Timeline's chronological presentation and ordering, with a counter plus non-wrapping up/down active-match navigation. Search results are a `TimelineMode`-equivalent read-only projection: no composer, edit, forget, membership, or favorite mutation; media viewing/playback remains available. Query, active match, and scroll state live at the app root for the current process session. Tags, locations, filters, categories, ranking, history, and text highlight are deferred.
- **Consequences:** This supersedes ADR-0006 and the old PRODUCT_SPEC §9 timeline-scoped search plan. Search adds no schema, backend, cloud, FTS, or third-party dependency. The existing MomentCard remains the presentation source of truth; text highlighting is intentionally deferred to avoid invasive card restructuring. Per ADR-0061 Search remains one of three top-level destinations — Home, Timelines and Search — with Home replacing Rediscover as the primary landing destination; its read-only projection, ordering, state ownership, and deferrals are unchanged.

---

## ADR-0035 — Deterministic generated covers for collection-card visual fallbacks

- **Date:** 2026-08-24 · **Status:** Accepted
- **Context:** Timeline Home and Rediscover collection cards previously used editorial copy or neutral surfaces when their bounded preview data had no image/video attachment. Those treatments made audio-only and text-only memories visually inconsistent while card geometry remained fixed.
- **Decision:** Collection-card fallback covers are rendered by one shared Compose `Brush` primitive. It deterministically hashes the logical card identity (`timeline-all`, Timeline ID, or Moment ID) into a curated tokenized palette; no gradient choice is stored. Image/video preview data always wins and reactively replaces the cover. Audio-only and text-only cards use the cover without fake media, icons, or waveform. Warm Journal uses restrained dark vibrant gradients; Monochrome Archive selects a neutral dark-gray palette. Normal Timeline MomentCard media presentation is unchanged.
- **Consequences:** The empty visual surface language in ADR-0029 is superseded. The behavior adds no schema field, bitmap, file IO, random/session state, or dependency, and it remains stable across recomposition and app restarts.

## ADR-0036 — Local custom-timeline name filtering on Timeline Home

- **Date:** 2026-08-24 · **Status:** Accepted
- **Context:** Global Search intentionally searches Moment title/content, while Timeline Home needs a small way to narrow its bounded custom-timeline cards without hydrating Moments or expanding archive-search scope.
- **Decision:** Timeline Home filters its already-observed summaries in presentation state using live, case-insensitive partial matching over custom timeline names only. Filtering preserves repository order, excludes the logical All timeline, and adds no SQL search query. The capsule follows the global Search container language but contains only a search glyph and `Search timelines...`; Timeline Home has no Back or Calendar action because neither navigation behavior belongs to this inline filter.
- **Consequences:** Query state remains process-local with the Timeline Home ViewModel, underlying ordering is unchanged, Moment fields cannot influence matches, and global Search remains untouched. Per ADR-0061 Timeline Home is no longer the app-wide root but is retained as the root of the Timelines destination, where this capsule continues to filter custom-timeline cards; it is not part of the Home surface's welcome → Rediscover row → All moments sequence.

## ADR-0037 — Omit empty On This Day from Rediscover

- **Date:** 2026-08-24 · **Status:** Accepted
- **Context:** The previous Rediscover behavior rendered an On This Day heading, date, and quiet empty message even when its bounded projection contained no eligible anniversary Moments. This reserved a large visual section for no content.
- **Decision:** Render On This Day only when its existing eligibility projection returns at least one Moment. When empty, omit its heading, date, placeholder, shelf, and reserved spacing; From Your Past follows Favorites using normal section spacing. When present, retain the approved section and expanded spacing before From Your Past.
- **Consequences:** Eligibility, previous-year matching, February 29 behavior, card presentation, and read-only navigation are unchanged. This supersedes only the empty On This Day presentation described by the Phase 7 acceptance text. Per ADR-0061 Rediscover is one horizontal row on the Home surface rather than a stack of sections, so an ineligible On This Day is omitted from that row's card order — with no heading, date, placeholder, or reserved spacing of its own — and From Your Past simply follows Favourites in the row.

---

## ADR-0038 — Global quick capture, bucketed All collage, unified card fade, and manual location

- **Date:** 2026-08-24 · **Status:** Accepted · §1 amended by ADR-0061
- **Context:** Relive's three active top-level roots need a consistent fast path into Moment creation; collection previews need a continuous media-to-information surface; the All card needs more varied visual recall without persisted recommendation state; composer media actions must not open platform surfaces behind the IME; and the existing persisted `ReliveLocation` model is not yet exposed for simple manual entry.
- **Decision:**
  1. Timeline Home, Rediscover root, and Search render one Relive-colored Material 3 extended `+ New` FAB above bottom navigation. Profile, Timeline detail, system-collection detail, gallery/viewer, camera, recorder, and modal/detail surfaces do not. The action always navigates to editable All, expands the existing inline composer, and requests title focus once; an already-expanded composer is left as the same single instance.
  2. Add Media and every Mic/Camera/Library or nested picker entry clear Compose focus and hide the software keyboard before platform media UI begins. Returning never requests keyboard focus and preserves the draft.
  3. `MediaToCardSurfaceFade` is the only collection-card transition primitive. Its tokenized gradient overlaps the visual/information boundary without changing card dimensions, outer shape/border, metadata spacing, or media choice. Real visual media and generated covers in custom Timeline, All, Favorites, On This Day, From Your Past, and shared Rediscover preview families use it.
  4. Custom summaries retain four visual previews. A dedicated SQLDelight All-collage query uses attachment identity plus `floor(epochMillis / 3 hours)` to sample at most nine image/video attachment references from the full visual archive without hydrating Moments. A pure resolver filters audio defensively, removes duplicate IDs, derives a varying count from 1 through `min(9, available)`, deterministically ranks candidates, and chooses a curated 1–9 grid from the same bucket and stable All key. The same bucket is stable across recomposition/navigation/configuration; the next activation/resume recomputes the current bucket. There is no timer, service, bitmap composition, saved collage file, database state, or full-resolution decode.
  5. The inline composer exposes a compact manual location-pin field directly below `DATE • TIME`. Non-blank input becomes the existing `ReliveLocation.placeName`; clearing it removes location. Existing insert/update SQLDelight plumbing persists it, editing can replace it, media flows preserve it, and successful Keep/reset clears it with the draft. GPS detection, permissions, Maps, and geocoding remain future work.
- **Consequences:** Quick capture remains one navigation path into the established All composer rather than a duplicate creation surface. All preview variety is local, bounded, reactive, and reproducible for three hours. Collection cards share one seamless surface language. Manual location ships with no schema migration or platform permission behavior. This supersedes ADR-0024/ADR-0033 only for where creation may be initiated, ADR-0033's static All-preview behavior only, and ADR-0008/Phase 4 only for GPS implementation timing; their storage, privacy, navigation, and moment-scoping rules remain historical and otherwise accepted. ADR-0061 amends §1: there is no Timeline Home root, Rediscover root, or bottom navigation to sit above — `+ New` is present on the Home surface in both of its states and on Search, hidden while the inline composer is expanded, and still absent from Profile, custom timeline detail, read-only collection detail, gallery/viewer, camera, recorder, and modal/detail surfaces. The "one navigation path into the established All composer" recorded above becomes one in-surface path: the action enters focused All moments and expands the existing single inline composer in place from the timeline rail, opening no chooser, modal, dialog, bottom sheet, or separate composer screen, and per ADR-0059 it requests no focus and opens no IME.

---

## ADR-0039 — Sequenced quick capture, portrait Android UI, and expressive media actions

- **Date:** 2026-08-24 · **Status:** Accepted · amended by ADR-0061
- **Context:** Quick capture could update the destination's composer state before All produced a visible settled frame, making the otherwise-approved inline transition appear abrupt. Android device rotation could also reconfigure the entire archive and capture surface, while the composer's functional Mic/Camera/Library reveal lacked the approved expressive hierarchy and equal action geometry.
- **Decision:** Global quick capture waits until All's Moment projection is no longer loading, yields one collapsed composed frame, and then changes the existing composer's target state; focus is requested on the following frame. Both global entry and the rail `+` use the same `AnimatedContent` vertical expand/fade transition with Relive slow/standard durations and standard easing. Android declares `MainActivity` portrait-only in the manifest; Compose never forces orientation and no sensor loop is added. CameraX capture, EXIF normalization, and recorded-video metadata handling remain unchanged. Portrait-only UI is also the iOS product requirement, but its supported-interface-orientation configuration remains a separately verified Xcode/Info.plist concern. The Add Media reveal remains one rounded `radius.lg` Relive card surface with three equal-width actions labeled Voice, Camera, and Media, using microphone, photo-camera, and gallery glyphs, Material press indication, exact action semantics, and `48dp` minimum targets.
- **Consequences:** Navigation, timeline position, composer identity, draft/persistence behavior, media capabilities, attachment previews, and keyboard-first media launching remain unchanged. Android rotation no longer recreates Relive into landscape, while capture orientation continues through its existing metadata pipeline. No dependency, schema change, Compose orientation forcing, iOS project mutation, or second animation system is introduced. ADR-0061 removes the quick-capture sequencing recorded here — waiting for All's Moment projection, yielding one settled collapsed frame, then requesting focus on the following frame — because it existed only to cover a navigation that no longer happens: `+ New` performs only the minimum scroll needed to seat the inline composer and its Keep Moment button in focused All moments, and per ADR-0059 no focus is requested. The shared rail `+` / global-entry expand/fade transition, the portrait-only decisions, and the Add Media reveal are unchanged.

---

## ADR-0040 — Floating top-level navigation toolbar

- **Date:** 2026-08-24 · **Status:** Accepted · amended by ADR-0061
- **Context:** The full-width bottom bar competes with Relive's editorial canvas and separates global navigation from its scroll context. The three active roots need compact, stateful navigation without changing their destination behavior or the separate creation affordance.
- **Decision:** Replace the full-width bottom bar with a bottom-left Material 3 `HorizontalFloatingToolbar`. It collapses on downward vertical scrolling to the active destination icon and expands on upward scrolling to show Timelines, Rediscover, and Search in their fixed visual order. Actions are icon-only with selected semantics and content descriptions. Quick capture is a matching bottom-right `HorizontalFloatingToolbar`: it collapses to Add and expands to a centered `+ New` with the same scroll state and expressive motion. Both controls use the semantic floating-surface token, accent actions, one shared responsive width calculation (`available width − horizontal margins − control gap − quick-capture width`), equal bottom alignment, matching navigation/IME insets, and root-list clearance. The navigation toolbar reserves equal minimum targets and slides a subdued accent-derived selected pill without overshoot; quick capture uses the prominent semantic action type. Touch exploration retains Material's expanded toolbar behavior.
- **Consequences:** This supersedes the prior bottom-navigation presentation only. Destination state, search return behavior, Quick Capture, Timeline detail hierarchy, persistence, and theme boundaries are unchanged. The implementation uses the existing multiplatform Material 3 experimental expressive toolbar API and adds no dependency. Per ADR-0061 the navigation toolbar's destination set is Home / Timelines / Search — there is no Rediscover destination icon, because Rediscover is a row inside Home, while Timelines retains Timeline Home and its custom-timeline detail screens unchanged — while its collapse to the active destination icon on scroll and expansion on reverse scroll are unchanged. `+ New` presentation is defined per Home state, not per raw scroll direction: it does not collapse to a bare Add icon merely because the user scrolled down into focused All moments, and it is hidden while the inline composer is expanded.

---

## ADR-0041 — Empty custom entry, direct collection-card boundaries, and saved-location display

- **Date:** 2026-08-24 · **Status:** Accepted
- **Context:** An empty custom timeline is a first-Moment creation state on every visit, while the previous one-shot entry intent left later empty visits collapsed. Collection-card media fades also obscured the intended boundary between a visual cover and its information surface. Saved readable location labels needed a consistent, privacy-preserving presentation rule.
- **Decision:** Every settled editable custom timeline whose observed Moment projection is empty expands its existing inline composer using the established animation; no opened-state flag is stored. All remains governed by its explicit global-New intent. Collection cards remove `MediaToCardSurfaceFade` entirely while preserving their deterministic generated fallback covers, media dimensions, and collage selection. Semantic tokens enlarge the lower information areas; only the title occupies flexible space with `Alignment.CenterStart`, leaving metadata in its existing lower row/area. Saved Moment location renders below date/time using the same eyebrow type, `textSecondary`, trimmed whitespace, and first-character-only capitalization derived from the persisted value.
- **Consequences:** Returning to an unsaved empty custom timeline again offers the composer; after the first Moment exists, normal entry is collapsed. No timeline, location, or cover data is migrated or duplicated. This supersedes ADR-0038's collection-card fade decision and the prior custom-timeline one-shot entry refinement only; global quick capture, generated-cover selection, card media sizing, and persistence behavior remain unchanged.

---

## ADR-0042 — Selective Material 3 interaction behavior and semantic haptics

- **Date:** 2026-08-24 · **Status:** Accepted
- **Context:** Relive's approved editorial components already define its visual identity, while generic dialogs, popups, platform-choice overlays, and undersized controls need stronger focus, accessibility, feedback, and dismissal behavior. Applying an experimental expressive theme globally would risk replacing settled layout and motion decisions.
- **Decision:** Material 3 and its expressive APIs are adopted only at component boundaries where they improve interaction behavior. Relive retains its timeline, cards, search capsules, composer fields, media presentation, floating-toolbar indicator, and camera chrome. Dialogs, menus, date picker, snackbar, transparent viewer controls, and the media-library modal use Material behavior under Relive colors, type, spacing, and semantic overlay/dialog/menu tokens. Shared Compose UI uses the semantic haptic cues `Action`, `Selection`, `ToggleOn`, `ToggleOff`, `Context`, `Confirm`, and `Reject` for direct controls and one-shot outcomes. Typing, scrolling, passive animation, normal card navigation, and routine Back/Close actions have no haptic. Android camera shutter feedback remains the existing success-timed native vibration, and the native iOS camera receives no shared feedback layer.
- **Consequences:** Relive gains consistent minimum targets, focus/error semantics, outcome feedback, and platform modal behavior without a global `MaterialExpressiveTheme`, a new dependency, a full-screen SearchBar, Add Media button groups, or pervasive spring motion. Experimental opt-ins remain local. Shared haptics may safely no-op when unsupported and must never be fired from recomposition.

## ADR-0043 — Global appearance mode with nostalgic palette defaults and custom-timeline overrides

- **Date:** 2026-08-24 · **Status:** Superseded in part by ADR-0050
- **Context:** Phase 9 needs an accessible light/dark appearance and a compact, nostalgic palette chooser without changing Relive's settled archive structure. The earlier Monochrome Archive and Film Memory names were conceptual placeholders and never had complete selectable token sets.
- **Decision:** Profile exposes an inline System/Light/Dark control and six app palettes: Original (Warm Journal), Evergreen, Lilac Dusk, Crimson Keepsake, Blue Hour, and Rosewood. The global palette and mode persist in native local preferences; System follows the live OS appearance. A custom timeline may store a nullable palette override in its existing theme column and change it from its editable header; null inherits the app palette and all timelines inherit global mode. Original light preserves the approved reference tokens. Other light/dark semantic colors and deterministic cover gradients derive from the accepted four-anchor palettes, retain accessible content contrast, animate with standard Relive motion, and never alter stored media. Legacy Monochrome Archive and Film Memory persistence values decode as Original.
- **Consequences:** Appearance changes recompose presentation tokens only; navigation, timeline geometry, Moment hierarchy, composer behavior, persistence, media, and search remain unchanged. No dependency or database migration is added. The earlier three-theme conceptual lineup in PRODUCT_SPEC §11, DESIGN_SYSTEM §19, ROADMAP Phase 9, and ADR-0010 is superseded only for selectable palette identity and global-mode behavior. Upgrade, Export, and other deferred Settings functionality remain outside this slice.

## ADR-0044 — Media & Storage is read-only archive insight

- **Date:** 2026-08-25 · **Status:** Accepted
- **Context:** Profile needs a truthful way to explain the local media archive without introducing storage-management behavior.
- **Decision:** Media & Storage calculates bytes only from persisted attachment references and Relive-owned file paths, categorizes Image/Video/Audio plus defensive unknown types, and performs file inspection away from the UI thread. Missing or inaccessible references contribute no bytes and never trigger deletion or repair. The screen is informational only: no optimization, compression controls, cleanup, deletion, storage permission, or device-wide scan is added.
- **Consequences:** Archive totals describe persisted Relive attachments rather than arbitrary app/device files. Duplicate storage references are deduplicated for bytes, while archive counts remain attachment-record counts. The feature adds no dependency or schema migration and leaves media ingestion unchanged.

## ADR-0045 — App behavior preferences use native local state and presentation-only scopes

- **Date:** 2026-08-25 · **Status:** Accepted · amended by ADR-0061
- **Context:** Phase 9 needs a small behavior-settings surface without duplicating Appearance, storage, privacy, or archive actions. Several candidate controls cross settled boundaries: ADR-0020 fixes editorial time to 12-hour `h:mm a`; ADR-0019/0023 require passive media tiles, lazy player creation, and one active playback owner; no approved user-initiated context exists for automatic audio.
- **Decision:** Add a domain-owned observable `BehaviorPreferencesRepository`, implemented by a dedicated Android `SharedPreferences` file and iOS `NSUserDefaults`, with shared stable-string codecs and safe defaults. Persist six controls: startup destination (Timelines default or Rediscover), explicit dirty-composer `×` confirmation, editable-Timeline location visibility, editable-Timeline tag visibility, Rediscover On This Day visibility, and Rediscover Favorites visibility. Startup is read only when the app root is created and yields to any future authoritative restoration/deep-link destination. Back continues preserving session drafts. Location/tag controls hide only normal editable All/custom Timeline presentation; composer/edit, Search, read-only system collections, persistence, and matching retain the data. Rediscover controls omit complete root sections while leaving eligibility/favorite state and bounded projections unchanged. `Use 24-hour time`, video autoplay, and automatic audio are deferred and absent from the screen because implementing them would conflict with the settled architecture above.
- **Consequences:** Behavior settings persist without an archive database migration and recompose only affected UI. No navigation tree recreation, scroll reset, draft clearing, media-state reset, player visibility tracker, or audio surprise is introduced. Preferences uses Material 3 Switch/RadioButton/dialog behavior, Relive semantic tokens, and existing `Selection`/`ToggleOn`/`ToggleOff` haptics without a global expressive theme. Per ADR-0061 the `Start Relive on` startup-destination control is removed and the persisted set becomes five controls: with a single root there is nothing to choose between, the app always opens at the top of the Home surface, and only restoration or a deep-link destination may target another surface. The Rediscover visibility controls omit the corresponding card from the Home surface's Rediscover row rather than a section of a Rediscover root, leaving eligibility/favourite state and the bounded projections unchanged.

## ADR-0046 — Local profile identity, native archive lock, and eligible Rediscover reminders

- **Date:** 2026-08-25 · **Status:** Accepted
- **Context:** The Phase 9 Profile needs truthful local identity, privacy, help, and About surfaces plus optional protection and reminders without accounts, archive disclosure, or new third-party dependencies.
- **Decision:** Store display name, managed photo reference, App Lock options, and Rediscover reminder opt-in in one observable native local repository; keep location visibility exclusively in `BehaviorPreferences`. Device authentication stays behind a shared capability and uses Android platform biometric/device credential APIs or iOS LocalAuthentication. Relive renders a neutral lock surface on cold start and qualifying foreground return while retaining app-level navigation, list, and draft state. Reminder scheduling stays behind a shared service: Android performs a unique daily WorkManager eligibility check and iOS maintains deduplicated annual month/day notification requests derived from prior-year local Moments. Notification content contains no archive fields. Mail requests contain only app/build, platform, and OS metadata, and the temporary support address is explicit release configuration.
- **Consequences:** No account, biometric library, notification library, location permission, schema migration, or new dependency is introduced. Authentication cancellation never enables App Lock; disabling reminders cancels scheduled and delivered Relive reminders. Legal URLs and the deliverable support address remain release configuration, and all native flows require physical-device QA.

## ADR-0047 — Cancellable return-to-newest control for timeline detail

- **Date:** 2026-08-27 · **Status:** Accepted · amended by ADR-0061 · superseded by ADR-0062 for custom timeline detail
- **Context:** The approved timeline intentionally encourages long chronological browsing, yet a reader who moves toward older Moments needs a fast way to return to the newest capture point without an abrupt position jump. The reference's fixed Edit/Share bar remains excluded and must not be reintroduced.
- **Decision:** All editable and read-only timeline-detail surfaces add one bottom-centered Material 3 Expressive small floating action button only after manual movement toward older Moments. It uses the existing floating-surface, accent, pill, icon/stroke, and minimum-touch-target tokens. Selecting it runs the LazyColumn's animated scroll to its terminal item; it never calls an immediate position jump for this action. During the motion the button hides. The first pointer-down in the scrolling content cancels and consumes that gesture, preserving the exact current position and preventing accidental content activation. Initial bottom placement, date navigation, selected-Moment navigation, and newly saved-Moment animation do not reveal the control. It remains hidden for empty or non-scrollable timelines and when the newest end is reached; a visible Snackbar offsets above it.
- **Consequences:** The control is an explicit additive exception to the timeline reference, not a replacement for its rail, composer, header, or existing top-level floating controls. It changes no domain model, persistence, navigation route, dependency, or public API. The shared UI owns only internal affordance state and one cancellable scroll job; reduced-motion scaling remains governed by Compose. Per ADR-0061 the control is scoped on the Home surface to focused All moments. That feed is newest-first, so movement toward older Moments is downward scroll and the control returns to the newest end at the head of the feed; it is hidden whenever the Rediscover row is visible, so it never competes with the upward scroll that restores the Home top state. There is no initial bottom placement to exclude, because Home opens at the top; date navigation, selected-Moment navigation, and revealing a newly saved Moment after Keep Moment still do not reveal it. Custom timeline detail keeps this ADR's existing behaviour.

## ADR-0048 — Contextual All-Moment actions and post-window organization

- **Date:** 2026-08-27 · **Status:** Accepted
- **Context:** The anchored Moment action menu interrupts All's continuous timeline and does not provide a way to file an already-created memory into a custom timeline. The 4-day policy is deliberately about editing and permanent deletion, while many-to-many membership is non-destructive archive organization.
- **Decision:** A long-press in editable All selects one Moment and swaps the standard header for a fixed-height contextual app bar using Relive fast emphasized fade/short-slide motion. Back exits selection. Before `createdAt + 4 days`, Edit and Forget appear; Add to timeline appears whenever custom timelines exist, including after the edit window. Its Material 3 picker is add-only and single-choice: assigned timelines are disabled, choosing an unassigned timeline calls the existing idempotent membership API, and success restores the normal header. Custom timeline detail retains its existing long-press interaction.
- **Consequences:** No schema, dependency, Moment mutation, or duplicate persistence is introduced. The 4-day rule remains authoritative for Edit/Forget and is rechecked at those action boundaries. Context, picker, success, and failure feedback use the existing semantic haptic vocabulary and Relive dialog/tokens.

## ADR-0049 — Android external shares enter the existing Moment composer

- **Date:** 2026-08-27 · **Status:** Accepted
- **Context:** A memory often begins outside Relive: in Files, Photos, a browser, or an audio app. Relive needs a local-first capture path without adding an alternate composer, saving before review, or exposing platform types to shared code.
- **Decision:** Android registers as a share target for plain text, images, videos, audio, and mixed multi-item shares. The Android edge immediately validates and copies accepted provider content into temporary Relive-owned files, then exposes a shared `IncomingShareGateway` request containing only text metadata and ordered `RawMedia`. The shared UI gives App Lock precedence, then presents All and custom timelines in a two-column grid. A selected timeline renders one settled collapsed Timeline frame before the existing inline composer merges the request and expands. Subject fills an empty title; text fills or appends to content; media appends in source order. A share is rejected in full when it has unsupported, unreadable, empty, oversized-text, or over-limit content. Cancelling before selection removes temporary files and returns to the source app; after claim, normal composer draft ownership applies.
- **Consequences:** One selected share becomes one reviewable Moment draft. Moment persistence, media normalization, memberships, themes, and the existing optional All-timeline assignments remain authoritative. There is no schema migration, new dependency, automatic save, arbitrary-document attachment type, background import, or iOS Share Extension in this iteration. Android `Intent`/URI APIs remain platform-edge details. Per ADR-0061 All is not a separate screen to render and settle: a share that targets All merges into the same inline composer on the Home surface, which the surface enters as focused All moments. The settled-collapsed-frame step recorded here applies to a selected custom timeline; App Lock precedence, the two-column selection grid, merge rules, rejection rules, and draft ownership are unchanged.

## ADR-0050 — Custom-timeline appearance is independent archive data

- **Date:** 2026-08-28 · **Status:** Accepted
- **Context:** The nullable custom-timeline palette override introduced by ADR-0043 made a timeline inherit the app palette, coupling timeline identity to a global presentation preference. The staged Timeline Theme redesign needs an archive-owned model before wallpaper and Moment-treatment UI are implemented.
- **Decision:** Every persisted custom timeline owns a non-null `TimelineAppearance` containing a `TimelineWallpaper` and `MomentTheme`. This model belongs to the domain and is stored in SQLDelight with the timeline; it has no reference to `AppearancePreferences` or app-level `ThemeReference`. Existing rows migrate to `WarmCream` plus a deterministic Moment theme: valid prior explicit palette identities are preserved under the timeline-owned enum and inherited/legacy placeholders become `WarmTerracotta`. The active timeline state exposes its appearance. Global appearance storage and behavior remain unchanged. This first part adds no wallpaper rendering, doodles, new Moment presets, preview UI, or customization screen.
- **Consequences:** Global mode or palette writes cannot overwrite a timeline's saved appearance, and timeline appearance writes cannot retheme Settings, Profile, navigation, or the global Material theme. The migration preserves timeline identity, cover, creation time, memberships, Moments, and existing valid explicit visual selections. Later redesign parts may expand rendering and customization while using this persisted boundary. ADR-0043 remains accepted for global appearance and is superseded only for nullable per-custom-timeline inheritance.

## ADR-0051 — Deterministic timeline-owned doodle wallpaper

- **Date:** 2026-08-28 · **Status:** Accepted
- **Context:** Custom timelines need a calm visual identity without duplicating bitmap assets, coupling their wallpaper to global appearance, or allowing a scrolling archive to regenerate or jump its decoration.
- **Decision:** Timeline Wallpaper is rendered in shared Compose UI as one viewport-fixed Canvas layer driven only by the active `TimelineAppearance.wallpaper`. One immutable normalized memory-doodle pattern supplies every palette; palette resolution changes only a low-contrast background and doodle tint, with restrained dark variants for readability. The reusable renderer serves both the timeline surface and Timeline Theme previews. Normal Moment rows are transparent so the layer is visible; rail-masking composer circles use the resolved wallpaper background. Moment-theme foreground behavior remains deferred.
- **Consequences:** No image assets, dependencies, schema migration, randomness, per-item drawing, or Android-only drawable logic is added. Different timelines can show different persisted wallpapers while global palette/mode writes do not change the saved wallpaper identity. Existing Moment text, dots, hearts, media borders, metadata, and interaction behavior retain their current tokens.

## ADR-0052 — Approved timeline wallpaper artwork is the visual source of truth

- **Date:** 2026-08-28 · **Status:** Accepted
- **Context:** The hand-drawn Canvas reconstruction in ADR-0051 did not reproduce the supplied wallpaper doodles faithfully enough for the approved Timeline Theme treatment.
- **Decision:** Bundle the six supplied wallpaper images as shared Compose resources and render the resource associated with the persisted `TimelineWallpaper` identity. The same resource surface remains shared by the timeline and Timeline Theme previews. The bitmap fills the available viewport deterministically and does not change persistence, navigation, Moment Theme behavior, or global appearance.
- **Consequences:** ADR-0051 is superseded only for the renderer implementation and its no-image-assets consequence. The wallpaper now preserves the exact supplied doodle composition, with no runtime generation, random placement, platform-specific drawable APIs, or additional dependency.

## ADR-0053 — Logical All timeline owns native-local appearance

- **Date:** 2026-08-29 · **Status:** Accepted
- **Context:** The Timeline Theme editor initially served only persisted custom timelines. The logical editable All timeline uses the same timeline UI and needs the same wallpaper customization without becoming a custom timeline or gaining archive membership data.
- **Decision:** All owns a non-null `TimelineAppearance` stored in the existing native `AppearanceRepository` alongside other app-local presentation preferences. Custom timeline appearances remain in SQLDelight. The shared Timeline Theme route accepts both editable scopes, while derived read-only collections remain excluded. All's appearance changes only timeline-owned wallpaper/Moment treatment and never the app palette, profile, navigation, Search, Rediscover, or system collections.
- **Consequences:** All gains the existing Timeline Theme action and reactive wallpaper rendering with no custom-timeline row, schema migration, membership, cover selection, or global retheming. Missing native preference values resolve to the existing Warm Cream / Warm Terracotta defaults.

## ADR-0054 — Versioned Google Drive app-data archive generations

- **Date:** 2026-08-25 · **Status:** Accepted
- **Context:** Relive's explicit Google Drive backup must remain private to the app, preserve durable archive relationships, and remain safe when large uploads or restores are interrupted.
- **Decision:** Backup uses a self-contained format-v1 bundle containing moments, custom timelines, memberships, tags, moment-tag links, attachment identity/order/storage references, and referenced media bytes. Installation-owned profile creation time is excluded because its SQLDelight immutability trigger must remain active and v1 restore cannot safely replace it. OAuth identity/tokens, client configuration, active jobs/resumable sessions, drafts, navigation/playback state, caches, and regenerable thumbnails are excluded. Objects are written to Drive's `appDataFolder` as immutable bundle, manifest, then index generations; the index is the promotion record and is created only after bundle/manifest upload succeeds. The prior promoted generation is never removed as part of candidate upload. Archive bytes are sent through an 8 MiB resumable stream rather than loaded into memory. Restore discovery accepts only a complete, version- and hash-verified indexed generation and activates it transactionally after staging.
- **Consequences:** Drive storage is not visible in normal My Drive browsing and the scope remains exactly `drive.appdata`. A failed or partial candidate cannot become current, and large mobile archives have bounded upload memory. SHA-256 provides integrity only; encryption and iOS transport remain deferred until their platform-safe recovery designs are implemented.

## ADR-0055 — Typography: full Material type scale, dark-mode halation weight, structural optical sizing

- **Date:** 2026-08-29 · **Status:** Accepted
- **Context:** A review of the Relive type system against the Google Fonts "Choosing type" guidance (checklist, emotive considerations, weights/grades, reliable typefaces, optical sizes) and the Material 3 `Typography` reference surfaced three gaps. (1) Only eight of the fifteen Material type roles were mapped from Relive tokens; the unmapped display and headline roles fell back to the Material default font (Roboto), so any Material component using them would break the "must not look like a default Material app" rule. (2) Dark themes reused the exact light-mode label weights; on a dark canvas light-on-dark text glares and its strokes visually bloat (halation), so SemiBold labels read heavier in dark mode than in light. (3) The bundled fonts are static cuts (Playfair Display Regular/Italic, Inter Regular/Italic/Medium/SemiBold), so a true variable `opsz`/`GRAD` axis is not available.
- **Decision:** (1) `reliveMaterialTypography` maps all fifteen Material roles: the eight existing Relive bindings are unchanged, and display/headline are derived from the serif `type.title` family with size-appropriate line height and tracking. (2) `labelWeightFor(isDark)` steps the four heavy label roles (`eyebrow`, `tag`, `action`, `prominentAction`) from SemiBold on light to the bundled Medium on dark; the lighter body and serif roles are left unchanged because no lighter cut is bundled and lightening 15sp+ text would trade legibility for a negligible glare benefit. The approved light Warm Journal reference tokens are byte-identical to before. (3) Optical sizing is satisfied structurally rather than with a variable axis: the high-contrast serif is used only at large (display) sizes and the text sans only at small sizes, and the small-caps roles already carry open tracking. The additive display/headline roles carry tighter tracking at larger sizes, emulating a large optical size. True `opsz`/`GRAD` control remains deferred until variable font binaries (with their OFL files) are added.
- **Consequences:** Material components can no longer leak a non-branded font. Dark-mode labels carry the same typographic color as light mode. No approved reference role changed, so the timeline reference remains authoritative. Adopting variable fonts later would replace the structural optical approach and the weight-step halation fix with real `opsz`/`GRAD` axes; that is a separate, explicitly approved decision.

## ADR-0056 — Typography redesigned into one modular editorial scale

- **Date:** 2026-08-29 · **Status:** Accepted
- **Context:** A UI/UX review found the type system read as amateur despite good font choices. The sizes clustered in a muddy middle (subtitle 14sp, body 15sp, prominent action 16sp were visually indistinguishable), the rhythm was broken (a cramped small cluster then a jump to 24/30/32sp with nothing between), several roles set no line height, and only the small-caps roles had any tracking. This supersedes the ad-hoc per-role sizing that preceded it, including the exact light Warm Journal reference metrics.
- **Decision:** Replace the sizes with one modular editorial scale, keeping the two established brand families (Playfair Display serif + Inter sans — no new binaries) and all existing token names so no call site changes. Reading `type.body` moves to 16 / 24 line height, the comfortable long-form size for a journaling app; the text roles step clearly (`caption` 13 < `subtitle` 15 < `body` 16 < `prominentAction` 16 by weight/context); the serif brand roles run `title` 24 → `coverTitle`/`wordmark` 30 → `display` 38. Two roles are added: `display` (hero/empty-state serif) and `caption` (small secondary sans). Every role sets an explicit size, line height, tracking, and weight; the large serif carries tight (negative) optical tracking and the small caps carry open tracking. The Material type-scale mapping (ADR-0055), the dark-mode halation weight step (ADR-0055), and structural optical sizing are retained and reseeded from the new roles.
- **Consequences:** The app has professional hierarchy and consistent vertical rhythm, and Material components inherit the full branded scale. `type.body` growing from 15 to 16sp increases text density slightly on every screen; layouts use `sp` and reflow. This intentionally changes the previously-authoritative light Warm Journal reference type metrics (per DESIGN_SYSTEM §0 this is the recorded human decision that supersedes them); the timeline reference image remains authoritative for layout, not exact type metrics. `ReliveTypography` gains two fields, built only through the `reliveTypography` factory.

## ADR-0057 — Typography "Kept": Fraunces replaces Playfair Display

- **Date:** 2026-08-29 · **Status:** Accepted
- **Context:** A senior UI/UX typography review (against the Google Fonts "Choosing type" guidance — checklist, emotive considerations, weights/grades, reliable typefaces, optical sizes — and the Material 3 `Typography` reference) found the type system competent but not yet delivering Relive's target feeling: personal, intimate, nostalgic, premium. The dominant weakness was the serif. Playfair Display is a high-contrast Didone whose thin hairlines and dramatic thick/thin read as fashion / wedding / magazine-cover — the exact "wedding invitation" trap Relive must avoid — and those hairlines fracture at small sizes over busy doodle wallpapers. Secondary issues: the wordmark's fashion-italic, uniformly heavy SemiBold labels, and reading `body` line height a touch tight for long-form journaling. Font family and pairing are design decisions the reference cannot settle; per DESIGN_SYSTEM §0 this ADR is the recorded human decision.
- **Decision:** Adopt the **"Kept"** direction: a warm old-style soft-serif (**Fraunces**) for every brand/emotional role paired with the existing neutral workhorse sans (**Inter**) for all reading and UI. Fraunces is bundled as two static roman cuts instanced from its variable font at a fixed 72pt display optical size (`opsz=72`, `SOFT=40`, `WONK=0`) — Medium (500) for brand roles and SemiBold (600) for the moment title — replacing Playfair's two cuts (no net binary increase). Static instances are used, not the variable font, because KMP variable-font axis support is inconsistent across platforms; this continues the project's structural-optical-sizing approach (ADR-0055) and keeps a live `opsz` axis deferred. Metric/character changes: `wordmark` becomes roman Medium (was fashion-italic); the moment `title` becomes serif SemiBold (the strongest text element — the opening sentence of a journal); reading `body` line height moves 24→26 for long-form comfort; `eyebrow` drops SemiBold→Medium and tracking 1.5→1.2 (calmer metadata); `tag` grows 11→12sp at Medium; `display` settles at 34/40. A new optional `type.dateLarge` role (serif Medium 28/32) is added for a future editorial day header — the token, Material mapping, and docs are provided but it is deliberately **not** wired into the timeline UI, and no date-group layout is introduced. The dark-mode halation step (ADR-0055) is preserved as a one-weight step but rebased to the calmer baselines: standard labels (`eyebrow`/`tag`/`action`) step Medium→Regular via `labelWeightFor`, and the CTA (`prominentAction`) keeps SemiBold→Medium via the new `prominentLabelWeightFor`. All fifteen Material roles remain branded; the additive serif display/headline steps are set at Medium to match the bundled cuts.
- **Consequences:** The app reads as an intimate editorial journal rather than glossy fashion, and gains a serif with genuine optical-size character that holds up at small sizes and over wallpapers. All screens re-render through existing `ReliveTheme.typography.*` tokens with no screen `.kt` changes; only the token layer, Material mapping, font resources/licensing, tests, and docs changed. `body` growing 24→26 line height slightly increases vertical text height on every screen; layouts use `sp` and reflow. `dateLarge` ships unused until a separate approved change wires an editorial date header. Playfair Display and its OFL are removed. This supersedes ADR-0056 only for the family/character and the metric/weight deltas listed above; ADR-0056's modular-scale intent and ADR-0055's Material mapping, structural optical sizing, and halation mechanism are retained and reseeded. Adopting the Fraunces variable font with a live `opsz`/`GRAD` axis remains a separate, explicitly approved future decision.

## ADR-0058 — M3 shape system: full 10-step radius scale, semantic shape holder, shape-morph library confirmed available

- **Date:** 2026-08-30 · **Status:** Accepted
- **Context:** Phase 0.3 of the staged Motion & Shape plan (`docs/MOTION_AND_SHAPE_PLAN.md`) requires the complete M3 corner scale, semantic shape tokens, and a verdict on whether the M3 shape-morph library (`androidx.compose.material3.MaterialShapes`, `androidx.graphics.shapes.RoundedPolygon`/`Morph`) resolves in `commonMain` under the pinned Compose Multiplatform `1.11.1` / `org.jetbrains.compose.material3 1.11.0-alpha07`. Phase 7 (favorite/recording/selection morphs) is blocked without this decision, and later shape work must have named token containers to draw from rather than inline `RoundedCornerShape(<dp>)` literals.
- **Decision:** (1) `ReliveRadii` exposes the full M3 10-step scale (`none 0, xs 4, small 8, medium 12, large 16, largeIncreased 20, xl 28, xlIncreased 32, xxl 48, full 999`). The legacy names `sm / md / lg / dialog / menu / pill` remain as `@Deprecated` computed aliases mapping onto the new tokens (`sm→small`, `md→medium`, `lg→largeIncreased`, `dialog→xl`, `menu→medium`, `pill→full`) so no call site breaks. The old `lg = 20dp` is M3 *large-increased*, not M3 *large* (16dp); its deprecation message flags the audit and a later step re-points each call site to the tier it actually wants. (2) A new `ReliveShapes` holder in `shared/src/commonMain/kotlin/com/vaibhav/relive/ui/theme/ReliveShapes.kt` derives named semantic shapes from `ReliveRadii` — `card` (medium), `dialog` (xl), `sheet` (asymmetric: top corners xl, bottom 0), `chip`/`button`/`pill` (full). It is bundled into `ReliveThemeTokens` and read through `ReliveTheme.shapes`. (3) `MaterialShapes`, `RoundedPolygon`, and `Morph` all resolve in `commonMain` under the pinned versions — verified by a temporary probe file that compiled clean against `:shared:compileCommonMainKotlinMetadata` and `:shared:compileAndroidMain`. No extra `androidx.graphics.shapes` dependency is added; it comes in transitively with `compose-material3`. No `expect/actual` wrapper is needed. Component styling is not touched in this step.
- **Consequences:** Every later phase draws corner radii and shape containers from the theme instead of literals, so a token change flows through the whole app. Phase 7 (shape morph — favorite, recording, floating-toolbar / bottom-nav selection) can proceed directly against `MaterialShapes` and `Morph` in shared code without a platform split. Phase 8.1 must resolve the `lg=20` audit per the deprecation flag before removing the alias. If a future Compose Multiplatform version drops the M3 shape library from common code, an `expect/actual` wrapper will be added at that point; no such wrapper exists today.

## ADR-0059 — Composer opens without automatic IME

- **Date:** 2026-08-30 · **Status:** Accepted
- **Context:** Automatic title focus on global quick capture immediately opened the keyboard while the inline composer was entering, which interrupted the desired calm review state.
- **Decision:** Opening the inline composer never requests title focus or opens the IME. The person explicitly taps a text field to begin typing. The existing IME inset and bring-into-view behavior still applies after that deliberate focus.
- **Consequences:** Quick capture opens as a keyboard-free review surface. ADR-0038 and ADR-0039 are superseded only for their automatic-focus behavior; their single inline-composer route, one collapsed-frame staging, and keyboard-safe media actions remain unchanged. Per ADR-0061 the collapsed-frame staging referenced here is itself removed for `+ New`, which expands the existing composer in place on the Home surface (see ADR-0039); this ADR's no-focus/no-IME decision is reaffirmed unchanged.

## ADR-0060 — One Free tier and one Relive Pro entitlement

- **Date:** 2026-08-31 · **Status:** Accepted
- **Context:** Relive needs monetization that protects the local-first archive promise without introducing multiple feature tiers, accounts, a backend, or hostage-like access to personal memories.
- **Decision:** Relive launches with Free and Relive Pro only. RevenueCat exposes one `relive_pro` entitlement, purchasable through `relive_pro_monthly`, `relive_pro_annual`, and the non-consumable `relive_pro_lifetime` product. Annual trial eligibility and duration are store configuration, never app behavior. Free permanently includes core capture and reading, Search, Favorites, On This Day, From Your Past, App Lock/privacy, and access to existing archive data. Pro gates Backup & Restore access, scheduled automatic backup and its cadence/network settings, creation beyond three custom timelines, and selection of premium appearance. Free appearance consists of Sunrise and Sunset palettes with Warm Cream and Blush Pink wallpapers; ADR-0071 later adds Warm Journal to the Free palette set. A retained premium appearance continues rendering after expiry. Product IDs, free appearance allowlists, entitlement ID, and platform public API keys are configuration values. If a public key is missing, entitlement resolution remains Free and purchasing reports unavailable without crashing.
- **Consequences:** Gates use a shared entitlement/policy boundary instead of store SDK checks in UI. Existing timelines and Memories can never be removed by expiry. RevenueCat and the platform keys are the only first-launch billing integration; RevenueCat Funnels and Stripe remain deferred. This supersedes ADR-0009 only for its "deferred" decision while retaining its interface and layering requirements.

## ADR-0061 — Rediscover and All moments are one continuous Home surface

- **Date:** 2026-09-03 · **Status:** Accepted · amended by ADR-0062 for custom timeline detail and 2026-09-23 greeting copy
- **Context:** Relive had three top-level destinations (Timelines / Rediscover / Search) with Timeline Home as the root inside Timelines, Rediscover as a second root, and the editable All timeline entered by tapping the All card on the Rediscover root. Reaching one's own memories therefore cost a destination switch plus a card tap, and the two halves of the same idea — resurfacing collections and the chronological archive — lived on different screens with different scroll positions. The product goal is a personal archive that reads as one continuous surface, not an application with sections. Separately, the All timeline was rendered by reversing a full `observeAll()` collection in presentation, which is acceptable for a screen entered on demand but not for the app's root.
- **Decision:** Rediscover's collection row and the All moments timeline become one vertically scrollable **Home surface**, which is the app's single root. Home renders, in one scroll container: a greeting (`Hey, {name}!!!` when a real profile display name exists, otherwise exactly `Hey!!!`), the subtitle `Your memories are waiting for you.`, the section heading `Relive your memories` with a horizontally scrollable collection row (`Favourites`, `On This Day`, `From Your Past`, `All Photos`), the section heading `All moments`, the inline composer at the chronological end of the feed, and the All moments feed itself. The app opens at the top of Home at scroll offset zero with no programmatic scroll on entry. Home has exactly two states — the top state and focused All moments — which are scroll positions, not screens: moving between them produces no navigation event, route change, or back-stack entry. The welcome and Rediscover sections leave the screen by scrolling and return **only** by manual upward scroll. `+ New` enters focused All moments and expands the existing inline composer in place from the timeline rail, scrolling only enough to seat it and its Keep Moment button; it never navigates, opens a chooser, or presents a modal, dialog, bottom sheet, or separate composer screen, and per ADR-0059 it requests no focus and opens no IME. Keep Moment resets and collapses the composer, holds the scroll offset with no app-initiated scroll of any kind, and renders the saved Moment in place; Back while composing collapses the composer under the same rule. Neither ever restores the top state. The All moments feed is **newest-first, bounded, windowed and paged** with incremental loading, read through a bounded anchored SQL projection; the root must never observe or hydrate the complete archive on launch. Rediscover is no longer a destination: the floating navigation toolbar's destination set becomes **Home / Timelines / Search**, where Timelines retains Timeline Home and its custom-timeline detail screens unchanged, and the `Start Relive on` behaviour preference is removed because Home is always the landing destination.
- **Consequences:** The archive reads as one surface, and the user's own moments are the root rather than two taps away. Newest-first ordering on Home is forced by the combination of opening at the top, loading only a bounded window, and keeping the composer a short scroll from `+ New`; an oldest-first feed would place the composer at the far chronological end, unreachable without loading the whole archive. Because the feed is newest-first, the composer's chronological end renders at the head of the feed and no rail continues above it, and the return-to-newest control's "toward older Moments" direction becomes downward scroll — which removes its former collision with the upward scroll that restores the top state. Custom timeline detail keeps its existing ordering, entry, and Back behaviour. This supersedes ADR-0024 **only** for Timeline Home being the app-wide navigation root — Timeline Home is retained as the root of the Timelines destination — and supersedes ADR-0033 **only** for All living in Rediscover and being entered from an All summary card, its rule that Timeline Home lists custom timelines only being retained. It supersedes ADR-0025 **only** for Rediscover being a second top-level destination — ADR-0025's bounded read-model requirements are retained and now extended to the All moments feed. It **amends** ADR-0015 (presentation-layer chronological order and the full-collection read, for the Home feed only), ADR-0038 §1 and ADR-0039 (quick-capture surfaces and the navigation handoff sequence), ADR-0040 (toolbar destination set, and `+ New` presentation defined per Home state rather than per scroll direction), ADR-0045 (removal of the `Start Relive on` preference), and ADR-0047 (return-to-newest scoped to focused All moments). ADR-0059 is reaffirmed unchanged. The fade-through pattern recorded in ADR-0060 (Rediscover collection navigation uses fade-through, not a container transform — the entry at the head of this file) continues to govern read-only collection routes opened from the Rediscover row, but no longer applies to reaching All moments, which is now a scroll.

## ADR-0062 — Custom timeline detail is a sliding-cover surface

- **Date:** 2026-09-03 · **Status:** Accepted
- **Context:** ADR-0061 rebuilt Home as one continuous surface where the welcome block and the Rediscover row are a backdrop and the All moments feed is a sheet riding over them, and deliberately left custom timeline detail untouched. That left the app's two editable timeline surfaces behaving differently in every respect the Home work had settled. On a custom timeline the cover hero was a fixed-height row in a column *above* the feed, so the feed had nowhere to travel and the cover could only stretch elastically on overscroll and spring back; the feed ran oldest-first with the composer at its tail and opened scrolled to the bottom; the return-to-newest control pointed down; and Back, timeline theme and calendar were drawn inside the cover, so they were part of what scrolled away. A custom timeline is the same act of reading one's own archive as Home, over a chosen cover instead of a greeting, and the two surfaces had no reason to move differently.
- **Decision:** Custom timeline detail becomes a **sliding-cover surface** running the same mechanism as Home. The cover photo moves out of the app-bar slot and is drawn as a backdrop *behind* the feed, which becomes a sheet with three resting places along one axis and no resting place between them: **expanded** (the cover fills the whole viewport and the feed is parked below the bottom edge), **resting** (the cover at its natural height with the feed starting below it, where the screen opens), and **focused** (the feed covering all of the cover but a strip at the top). The sheet stops below the pinned controls rather than at the top of the screen, so the timeline never runs under Back, theme or calendar even at the end of its travel; the strip left above it is the cover's. Below `resting` the sheet is list scrolling; above it the leftover pull drives the sheet off the bottom edge while the cover grows by the same distance, which is exactly what it needs to fill the viewport. A lifted gesture settles to whichever end its direction was travelling toward. The cover trails the sheet under the same parallax and dim as Home's welcome block. The feed is presented **newest-first** with the inline composer at its **head**, the surface opens at the top of its cover with no entry scroll and performs no app-initiated scroll after Keep Moment, and the return control becomes the bottom-centered **up** arrow that returns to the head of the feed at a visible pace. Back, timeline theme and calendar are lifted out of the cover and **pinned above the sheet**: the timeline passes underneath them and never covers them, the moment-selection bar occupies the same pinned slot so a long-press shifts no layout, and the feed reserves top padding for their clearance. The sheet carries **no heading**: in the focused state nothing on screen names the timeline, which is the intended reading state. The elastic cover stretch is removed here, because it and the expansion want the same overscroll. `Timeline.All` opened as a detail screen keeps its generated collage hero, its elastic stretch and its oldest-first order, and is unaffected.
- **Consequences:** The two editable timeline surfaces now move identically, and the mechanism itself — the expansion state, its nested-scroll connection, the settle rule, the scrolled-into-backdrop derivation and the horizontal bleed — is extracted from Home into one shared implementation both call, so they cannot drift apart. This **supersedes ADR-0015 for custom timeline detail**, which was that ADR's remaining scope: presentation no longer reverses the repository's newest-first emission for a custom timeline, the composer is no longer the terminal item, and neither the first-snapshot `scrollToItem` nor the growth `animateScrollToItem` applies — an explicitly selected Moment is still navigated to, which is the one app-initiated scroll besides calendar navigation that this surface keeps. ADR-0015 now records only the read-only system collections. It **supersedes ADR-0047 for custom timeline detail** (the down-pointing return-to-newest control is replaced by Home's up-pointing one) and **amends ADR-0061**, whose statements that custom timeline detail keeps its existing ordering, entry, Back behaviour and chrome no longer hold; every other part of ADR-0061 stands, including that Timeline Home remains the root of the Timelines destination and that a custom timeline carries no floating navigation toolbar and no `+ New` — its composer is reached inline on the rail. ADR-0059 is reaffirmed unchanged. Because the cover is behind the feed, tapping it to change the cover photo is available while any of it is visible and not in the focused state, which is the only reachability this surface loses. No domain model, persistence contract, navigation route, dependency or public API changes; the repository's newest-first ordering contract is untouched, as ADR-0015 required.


## ADR-0063 — Opening a custom timeline is a container transform from its card

- **Date:** 2026-09-03 · **Status:** Accepted
- **Context:** ADR-0062 made a custom timeline's cover photo the surface the whole screen is built on. Tapping that timeline's card on Timeline Home, however, swapped the two screens with no transition at all, which reads as a cut: the card carrying the cover photo disappears and a screen carrying the same cover photo appears, with nothing connecting them. ADR-0060 rejected a container transform for Rediscover collections on the grounds that the card's cover is **not** retained in the destination, so morphing one into the other asserts a continuity that does not exist. A custom timeline is the opposite case — the card's cover photo *is* the destination's cover photo, at the top of the destination, and the card's title *is* the destination's title.
- **Decision:** Tapping a custom timeline's card on Timeline Home opens its detail screen with an **M3 Expressive container transform**: the card's bounds morph into the screen's over `long2` on the emphasized curve, with the fade-through variant this app already uses for quick capture — the card fades out over the first half and the screen fades in over the second, so neither is seen enlarging inside the morphing container. The shared element is keyed by the timeline's id, so one card can only ever morph into its own timeline. Back runs the same transform in reverse, returning the screen to the card it came from. The route's own swap cross-fades over the same duration, because both halves must be composed for the container to morph. A detail screen reached any other way — quick capture, an incoming share, a Search result — is unaffected and keeps the transition it already had, and reduced motion drops the shared element entirely.
- **Consequences:** The cover photo is now continuous from the card to the screen, which is the continuity ADR-0060 requires before a container transform is permitted; that ADR is **reaffirmed, not superseded** — it still governs Rediscover collection routes, whose covers are not retained. The transform is a second use of the transform helper pattern established for quick capture rather than a new mechanism, and the two are keyed apart so they cannot collide. Timeline Home must be able to give the tapped card a shared-element modifier, so it takes one from its navigation host rather than constructing shared-transition scopes of its own; every other caller of the card gets the no-op default. No domain model, persistence, navigation route, dependency or public API changes.


## ADR-0064 — Home Rediscover cards are full-bleed covers with an overlaid focal title

- **Date:** 2026-09-03 · **Status:** Accepted
- **Context:** The Home Rediscover row's collection cards were split into a visual region above an opaque information surface carrying a title and a metadata line (a moment count or date). Inside the multi-browse carousel the lower band read as administrative weight on cards whose whole job is invitation, and on masked medium/small items it was dead surface under imagery. The generated fallback covers were also seeded from the palette's dark roles in both modes, so a media-less card matched neither the light canvas nor the `+ New` accent that anchors the surface, and every cover choice was permanently deterministic — the same card wore the same gradient forever, which works against a row named Rediscover.
- **Decision:** Four connected changes, scoped to the Home surface's Rediscover collection row only. **(1) Full-bleed cover:** the cover fills the entire card (`rediscover.compactCardHeight`, the former media-plus-info footprint); there is no information surface. A collection with visual preview media shows exactly one image or video attachment; one without wears a generated gradient. **(2) Overlaid focal-only title:** the card's sole text is its one-line title, drawn at the lower edge over a black vertical legibility scrim (`ReliveCoverLabelScrim`) in white; scrim and title share one layer whose alpha follows the carousel's focal growth, so a cover is dimmed exactly while text is overlaid on it and masked cards stay pure, undimmed imagery. Row cards carry no moment count, date, or other metadata. **(3) Accent-derived, mode-aware gradients:** `generatedCoverPaletteFor(palette, isDark)` derives six gradient pairs from the active mode's workhorse accent — the `+ New` colour — deepened toward black in light mode and lifted toward white in dark mode, all stops opaque and strictly on that side of the accent. **(4) Session-shuffled selection:** the row mixes one per-launch shuffle value into every cover seed before the shared deterministic hash (`stableCoverIndex`) picks the gradient or the preview attachment, so cover assignments are re-dealt each app launch but hold still within a session, including as cards leave and re-enter composition.
- **Consequences:** This amends the §14 media/surface boundary rule, the §19.1 "never random" rule and its All Photos bucketed-collage cover, and the §20 card family/metadata description of `DESIGN_SYSTEM.md` **for the Home Rediscover row card family only**. Every other collection-card family — Timeline Home cards, the shelves inside opened collections, `RediscoverMomentVisual`, and the external-share timeline picker with its bucketed collage — keeps the two-part anatomy and fully deterministic covers. The mode-aware palette applies to every `ReliveGeneratedCover` consumer, which all belong to the Rediscover preview family; covers are no longer identical across light and dark modes, and follow the palette's accent by construction. No domain model, persistence, navigation, dependency, or public repository API changes; the row's height, width, spacing, carousel behavior, and hoisted-state contract with Home's drag-proxy window are unchanged.

## ADR-0065 — Rediscover collections are sliding-cover surfaces their cards morph into

- **Date:** 2026-09-03 · **Status:** Accepted
- **Context:** ADR-0064 made every Home Rediscover card one full-bleed cover, but the screens those cards open still belonged to an older generation of the app: an app bar carrying the collection's name over an oldest-first feed that opened scrolled to its newest moment at the bottom, reached by a fade-through, while a tap on a card at rest never arrived at all — Home's transparent backdrop-window item owns the hit test over the row and proxied only horizontal drags, so card taps died in it unless the backdrop was expanded. Meanwhile ADR-0062 had settled what an archive-reading surface is (a sheet riding over a backdrop, newest-first, no heading naming the surface), and ADR-0060's objection to a container transform — the card's cover is not retained in the destination — stopped being true the moment the collection screen could open under the very cover its card wears.
- **Decision:** Five connected changes. **(1)** Home's backdrop window forwards taps as well as drags: the carousel registers each card's visible masked bounds in root coordinates, and a tap on the window opens the card under it, so cards work in every backdrop state. **(2)** Read-only system collections present the repository's newest-first emission unchanged — the presentation-layer reversal is deleted — and every collection opens at the top of its surface with no entry scroll and no card-carried selected moment; From Your Past keeps its curated daily order. **(3)** The read-only collections become **sliding-cover surfaces** running exactly the ADR-0062 mechanism: the cover behind a sheet with expanded/resting/focused states, pinned controls above the sheet, no app-bar heading anywhere (the `SystemCollectionHeader` component is deleted). Their pinned slot holds **Back alone** — no timeline theme, no calendar — and the cover accepts no update tap. **(4)** The cover a collection opens under is **the cover its Home card was wearing**: the tap resolves the card's session-shuffled pick (one preview attachment or the accent-derived gradient) and carries it through the route, so card and screen show one continuous image; the cover hero titles the surface the way a custom timeline's cover does. **(5)** Opening a collection from its card is an **M3 Expressive container transform** — the same fade-through-variant morph, `long2` on the emphasized curve, that ADR-0063 established for custom timeline cards, keyed per collection so a card can only morph into its own screen, run in reverse by Back, and dropped entirely under reduced motion (which keeps the fade-through).
- **Consequences:** ADR-0015 is now fully retired: no surface reverses the repository's newest-first order in presentation. ADR-0060 is **superseded for the card-opened collection routes** — by its own rule, since the cover is now genuinely continuous from card to screen — and continues to govern nothing beyond the reduced-motion fallback; the Home ↔ All-theme route keeps its fade-through. ADR-0062 is **extended** to the read-only collections, minus the three affordances they deliberately lack (theme, calendar, cover editing), and its no-heading rule now covers them. ADR-0064's cover resolution becomes shared infrastructure: one function feeds the card, the opened screen, and the transform's continuity. The read-only collections still mutate nothing; no domain model, persistence, or repository API changes.

## ADR-0066 — Optional Moment feelings with Mood insights on Home

- **Date:** 2026-09-03 · **Status:** Accepted
- **Context:** Relive records what happened but nothing about how it felt, and the archive has no reflective surface that reads across Moments. The approved design (PRODUCT_SPEC §10A) adds a three-value feeling captured through a quiet post-save prompt, a small face on the Moment's print card, and a week-over-week mood bar that opens chart insights. Three placement questions needed settling: where the feeling write lives relative to the 4-day edit window, where the bar lives on a Home surface whose spec says moving between states is scrolling only, and how insights open without adding a destination, a modal, or an archive-wide read.
- **Decision:** A Moment gains one optional persisted `feeling` (`Great` / `Good` / `Low`), stored as a nullable `TEXT` column on `moments` and absent by default forever — skipping the prompt is a first-class path, and no Moment is ever backfilled. The feeling write is a dedicated repository operation like the favorite toggle, independent of `updateEditable` and therefore of the 4-day window; editing never clears a feeling and never re-prompts. The post-save prompt renders inline beneath the just-saved Moment card, only for new saves on editable surfaces, and is dismissed by choosing, its `×`, a subsequent save, or leaving the surface. The mood bar joins Home's **backdrop**, beneath the welcome block, and is revealed by the backdrop's existing **expanded** resting place (ADR-0061/0062's sliding-backdrop mechanism) rather than by a new gesture or a new always-visible section: at rest Home is unchanged. Home owns a normalized `BackdropExpansionState` progress mapping: the salutation scales 1.0×→1.32×, the inline and second-line name layers crossfade only in 0–10% and 80–100% intervals, and the subtitle remains attached to greeting geometry. The full-size Mood bar reveals together from 25–80%, with its measured slot allocated progressively beneath the subtitle so lower content moves continuously and collapsed Home reserves no gap; it does not scale and is non-interactive/non-accessible while substantially hidden. Home gesture tracking is direct through 80%, then smoothly resists outward travel to a 0.3 response ratio at full expansion. Reverse travel is direct from its current rendered position, and settlement keeps the existing destination choice but uses a velocity-preserving critically damped spring that a new drag cancels immediately. `Standard` remains the default backdrop behavior and retains Timeline physics. Tapping the revealed bar opens **Mood insights** as a full-screen scrollable surface layered over Home (not inside the backdrop behind the feed): it lays the composition out top to bottom — `Mood over time`, the bar between the two charts, `Weekly mood`, the day split, the count tiles — generously spaced with its own scroll, so the charts get room and never compete with the feed for touch. An earlier draft grew the insights in place around a fixed-anchored bar inside the backdrop; that was replaced because the backdrop sits behind the list and cannot scroll or own touch cleanly, which the roomier, taller charts require. Back closes insights before any other Home Back behavior. Aggregation is local arithmetic in a pure calculator over a bounded projection — `(created_at, feeling)` pairs since a ~6-month cutoff — never a hydration of the archive; weeks run Sunday–Saturday on the device-local calendar, a week's verdict is the mean feeling score of its felt Moments, and windows with no felt Moments render quiet em-dash/empty forms. The faces are drawn primitives (gradient-shaded spheres, like the existing push-pin), fixed warm gold in every palette the way the print card stays white and the audio tile stays black; their idle bob and blink are disabled under reduced motion.
- **Consequences:** The `moments` schema gains its first nullable enrichment column (migration `4.sqm`); whole-database backup bundles carry it with no format change, and an older bundle restored into this build migrates on open. `MomentSaveOutcome.Succeeded` now carries the saved Moment's id and whether it was a new save, which is what anchors the prompt. Home's spec statement that the top state and focused All moments are its only named states is **amended, not broken**: the bar reveal rides the already-shipped expanded resting place, and insights open as a scrollable surface layered over Home — not a navigation event, route, or back-stack entry — Back handling extends Home's precedence list with "open Mood insights" ahead of selection and composer. Insights reads stay bounded, so ADR-0061's no-archive-hydration rule extends to this surface. A behavior preference to suppress the post-save prompt is deferred until asked for; Places/Tags-correlated insights remain deferred with the rest of the Rediscover read models. No dependency, backend, AI, or analytics capability is introduced (ADR-0001 unchanged).

## ADR-0067 — Capture-focused daily reminders (two kinds, up to three a day)

- **Date:** 2026-09-04 · **Status:** Accepted · amends ADR-0046
- **Context:** ADR-0046 shipped a single daily eligibility-gated Rediscover reminder with one static line. Relive should also invite the daily habit of *adding* a moment, not only revisiting old ones, and the product owner asked for two to three reminders a day that are tempting enough to open. The change must hold ADR-0046's two invariants: notification content carries no archive field, and reminder scheduling stays behind the shared `RediscoverReminderService`.
- **Decision:** Reminders become a **capture-focused set of up to three per day**. Android schedules three unique daily `WorkManager` jobs at fixed local hours — a Capture nudge at ~10:00, a Rediscover nudge at ~13:00, and a Capture nudge at ~19:00 (2:1 capture-weighted) — each an initial-delay-to-time periodic request carrying a `kind` and a per-slot `variant`. Two kinds: a **Capture** nudge that always applies and, when tapped, opens the Home quick-capture composer via an explicit `com.vaibhav.relive.ADD_MOMENT` activity intent; and the existing **Rediscover** nudge, which keeps its On This Day eligibility gate and opens Home. Copy is a small pool of **static, generic** messages chosen deterministically by day-of-year plus the slot variant, so it rotates day to day and morning differs from evening; a pure shared `selectReminderCopy(kind, seed)` holds the pool and is unit-tested to contain no interpolation slot. **ADR-0046's no-archive-field rule is retained unchanged** — the selector is never given Moment data. One Profile opt-in still governs the whole set; the Profile row is renamed **"Rediscover notifications" → "Reminders"** and its copy updated to describe both kinds while repeating the no-content promise. iOS reminder parity is deferred; the existing annual On This Day requests are unchanged.
- **Consequences:** No new library, permission, schema, or dependency for reminders (`POST_NOTIFICATIONS` is already declared). Disabling cancels all three unique works and both notification ids. The two Capture nudges share one notification id, so an ignored morning nudge is replaced rather than stacked; the Rediscover slot keeps the original unique work name so an existing schedule updates in place. **PRODUCT_SPEC §673's "Rediscover notifications" label becomes "Reminders."** The Capture nudge reuses the same in-app entry point as the global `+ New`, added as a process-lifetime `QuickCaptureRequestBus` field on the container that `MainActivity` signals and `App` observes. Physical-device QA is required for delivery, permission, and tap routing.

## ADR-0068 — Android home-screen Quick Capture widget (Glance)

- **Date:** 2026-09-04 · **Status:** Accepted
- **Context:** Relive had no home-screen presence. The product owner asked for an attractive widget. The home screen is the most publicly visible surface on the phone, so it must disclose no archive content, and adding it must respect the local-first, no-new-capability posture (ADR-0001).
- **Decision:** Ship one Android app widget — **Quick Capture** — built with **Jetpack Glance** (`androidx.glance:glance-appwidget`), Relive's first `<receiver>` and first non-stdlib UI dependency, introduced deliberately for this feature. The widget renders a single calm card that **follows the palette and light/dark mode the user has chosen in Appearance**: it reads the saved `AppearancePreferences` at render time and resolves colours through `paletteFor(theme).roles(isDark)`, and `MainActivity` calls `updateAll` whenever that choice changes so placed widgets restyle live. (At the time of this decision the default a fresh install showed was Ink & Lilac; ADR-0071 later changes the fresh-install default to Warm Journal.) It shows only a generic prompt ("Capture a moment / Add today to your archive"), **never a Moment**, and reads no archive content from the database. The entire surface is one tap target that launches `MainActivity` with the `ADD_MOMENT` action and lands in the Home quick-capture composer — the same shared entry point the Capture reminder uses. A monochrome notification/brand mark (`ic_relive_notification`) is added as a **placeholder**, because the launcher icon is still the stock Android template. iOS WidgetKit is out of scope (this Windows host cannot build iOS).
- **Consequences:** One new dependency (`androidx.glance`) and one exported receiver; no backend, account, analytics, or archive read is introduced, and the widget cannot leak private content because it never holds any. The Glance dependency is isolated to `androidApp`; shared code is untouched by the widget. The widget follows every resolvable palette and the light/dark mode; because a palette change requires the app to be in the foreground, observing `appearanceRepository.preferences` in `MainActivity` covers every real change. The placeholder notification mark should be replaced when a real one exists. Widgets are net-new scope beyond the ROADMAP, added at explicit product-owner request. Physical-device QA is required.

## ADR-0069 — The external-share picker is the Timelines screen minus its controls

- **Date:** 2026-09-04 · **Status:** Accepted
- **Context:** ADR-0049 gave incoming Android shares their own selection surface: a hero preview of the shared content above All and custom timelines browsed as cover cards. The Timelines screen has since been redesigned — floating controls over the canvas gradient, a search field, a `YOUR TIMELINE` heading, and full-width cards that morph into their timeline (ADR-0063) — and the picker was left behind, so sharing into Relive arrived at a surface that no longer looked or moved like the app it was entering. The redesigned Timelines screen also lists custom timelines only; All is reached by scrolling Home, not by tapping a card.
- **Decision:** The picker becomes the Timelines screen with everything that does not choose a destination removed. It keeps that surface's canvas, grain, search field, `YOUR TIMELINE` heading, card family, order, empty and no-match states, and its container transform; it drops the profile affordance, the create control, the floating navigation, and the long-press rename/delete selection, so a card carries exactly one gesture. The wordmark is replaced by a `Choose a timeline` heading and Back sits where the profile affordance does, returning to the app the content came from. The hero preview of the shared content is deleted — the share is reviewed in the composer it lands in, not before choosing. Following the redesigned surface it replicates, the picker lists **custom timelines only**, so a share can no longer target All. Choosing a timeline runs ADR-0063's container transform from the picker's card into that timeline's detail screen, keyed by the timeline, with the share route's swap cross-fading over the morph on the same `long2` emphasized curve; the payload then merges into the inline composer exactly as before. Because the picker is the share's first screen, the list is present from the first frame: the Timeline Home skeleton covers summary loading, an error fades in over the list body with Try again / Close, and a card chosen while the payload is still being read is held lit and commits when the payload lands rather than being swallowed.
- **Consequences:** ADR-0049 is **superseded for the selection surface only** — its platform-edge normalization, temporary-file ownership, merge rules (subject to title, text to content, media appended in source order), rejection rules, claim, and draft ownership are unchanged, as is the settled-collapsed-frame handoff into the composer. Its two-column grid, its All-first ordering, and the share-targets-All path through the picker are retired, and with them the `shareTimelinePicker` dimension tokens and this surface's use of the bucketed All collage cover (which the All timeline's own cover hero still uses). PRODUCT_SPEC §`Android external share capture` and DESIGN_SYSTEM §15/§19.1 are amended accordingly. A device with no custom timelines has no share destination and must create one in the app first; this follows directly from replicating a surface that carries no creation control. No domain model, persistence, repository API, navigation destination, or dependency changes: the picker still hands the host one timeline and one payload.

## ADR-0070 — Profile About and Help have distinct explanatory roles

- **Date:** 2026-09-09 · **Status:** Accepted
- **Context:** The existing Profile Help & Feedback and About Relive pages mixed product explanation, temporary screenshot placeholders, unfinished legal rows, and diagnostic mail actions. Their overlap made it unclear whether a person should browse About to learn Relive or open Help to solve a problem.
- **Decision:** About Relive is the complete descriptive app guide: one editorial product-story hero, tokenized feature cards for capabilities that actually ship, version/build, Privacy Policy and Terms of Service links supplied from the existing release configuration, and open-source licenses. Help & Feedback is a task-oriented support center: expandable answers for creating, media, timelines, finding memories, Rediscover, backup, privacy, and Pro; a release-configured `RELIVE_SUPPORT_EMAIL`; and separate feedback/problem-report mail actions. The support address travels through each platform bootstrap to the shared app container, defaults blank in source control, and disables contact actions when unavailable. Diagnostic mail retains the ADR-0046 rule of app/build, platform, and OS metadata only.
- **Consequences:** The Profile hierarchy, archive data, persistence, entitlement policy, and legal URL source remain unchanged; no dependency, backend, analytics, or account is added. About has no raster screenshots or placeholder art, and all its feature copy remains truthful about local-first storage and optional backup. Physical-device QA still verifies URI and mail handoffs on both platforms.

## ADR-0071 — Warm Journal matches the launcher icon and leads Free appearance

- **Date:** 2026-09-11 · **Status:** Accepted
- **Context:** Relive's new launcher icon established a distinct cream-paper and leather-brown identity, while the app opened in an unrelated palette and the palette picker mixed Free choices after premium choices.
- **Decision:** Add Warm Journal as an app palette using cream paper, leather brown, deep brown ink, and restrained tan details derived from the launcher icon, with a corresponding accessible chocolate-and-cream dark variant. Warm Journal is the default only when no palette preference exists; an explicitly saved palette remains unchanged. Warm Journal joins Sunrise and Sunset in Free. The Appearance row lists Warm Journal, Sunrise, and Sunset first, followed by the existing selectable premium palettes. The retired Original/Ink & Lilac identity remains decodable so an existing saved choice is preserved, but remains unavailable for new selection.
- **Consequences:** The app, Android Quick Capture widget, generated covers, and Material colors resolve to Warm Journal on fresh installs because they share `AppearancePreferences` and the theme-token resolver. No schema, dependency, navigation, timeline appearance, or stored media changes. Palette persistence gains one stable `warm_journal` value, and existing explicit preferences continue to round-trip.

## ADR-0072 — The launcher icon follows the selected global palette

- **Date:** 2026-09-11 · **Status:** Accepted
- **Context:** Warm Journal aligned the default app palette with the new diary launcher icon, but selecting another palette left the installed icon wearing Warm Journal colors. The product owner asked for each theme to have its own icon while retaining one icon across that theme's Light and Dark modes.
- **Decision:** Give every global palette one launcher-safe recoloring of the same diary composition. Warm Journal keeps the existing primary icon unchanged. Sunrise, Sunset, Evergreen, Ember & Aqua, Plum & Gold, and Rose & Sage receive matching theme-gradient variants; retired Original receives a compatibility variant for an existing saved preference. The selected global palette maps to a platform icon identity through a shared `LauncherIconController`; global System/Light/Dark mode is not an input. Android exposes mutually exclusive launcher `activity-alias` entries and iOS packages alternate app-icon sets. The app synchronizes the icon when the persisted palette changes and on launch.
- **Consequences:** Palette selection now changes both app presentation and launcher artwork, while a mode-only change leaves the icon untouched. No preference or database field is added because the icon is derived from the already-persisted palette. Android launchers may take a moment to refresh an alias, and iOS owns any system confirmation associated with changing an alternate icon. Timeline-owned appearance cannot change the launcher icon. No dependency, backend, archive data, navigation behavior, or entitlement rule changes.

## ADR-0073 — A versioned first-launch introduction precedes Home on fresh empty installs

- **Date:** 2026-09-10 · **Status:** Accepted · amends ADR-0061 for first launch only · four-page presentation superseded by ADR-0085
- **Context:** Relive's one-root decision correctly makes Home the ordinary launch destination, but a fresh empty archive currently arrives with no explanation of Moments, timelines, local-first privacy, or Rediscover. The product owner supplied a motion reference and explicitly requested a similarly minimal, illustration-led onboarding while retaining Relive's settled identity and architecture.
- **Decision:** A fresh, empty installation receives one skippable four-page shared-Compose introduction before Home: capture thoughts and media; understand All moments and custom timelines; understand on-device privacy and optional App Lock; and understand Rediscover. It uses the active Relive palette, original transparent keepsake artwork, one idea and one primary action per page, a fixed wordmark/progress thread, tokenized sequential fade-through, and the existing reduced-motion gate. Completion and Skip persist `completedOnboardingVersion = 1` in platform-native install preferences outside SQLite; partial page position is not persisted. App Lock remains the outer gate. Incoming shares and authoritative deep links bypass onboarding without completing it. On initial rollout, bounded Profile counts migrate any installation with a Moment or custom timeline directly to completed, while an empty installation may see the introduction. Onboarding asks for no permission and does not configure Reminders, App Lock, backup, accounts, or sync.
- **Consequences:** ADR-0061's statement that the app opens at Home is amended only for the first ordinary launch: completing onboarding reveals the same Home top state at offset zero, and every later ordinary launch remains unchanged. `App.kt` gains a gate around the normal unlocked root; a small repository interface and Android/iOS native preference implementations carry install-level completion. The archive schema, navigation destinations, Home state model, notification and authentication flows, and bounded-loading contracts do not change. Four project-owned raster heroes add presentation assets without adding a runtime dependency. At the product owner's explicit follow-up request, those heroes use a larger centered presentation, stronger tactile 3D depth, and dedicated larger onboarding type roles; reduced motion still removes all perspective movement. Phase 11 is entered early as an explicit approved override; all earlier in-progress roadmap phases retain their status.

## ADR-0074 — Runtime builds contain no sample or QA archive data

- **Date:** 2026-09-11 · **Status:** Accepted
- **Context:** Android debug startup automatically inserted a fixed Rediscover QA dataset, and an unused debug-only in-memory repository retained five sample Moments. This made a normal development install look populated and could leave fake records in its persistent archive after the utilities were removed.
- **Decision:** Remove the automatic seeder, its controls, the sample repository, and their utility-specific tests. Debug and release builds now use the same empty SQLDelight archive until the person creates or restores real data. Migration `5.sqm` removes only the fourteen fixed IDs owned by those retired utilities, their database relationships, and the now-unused `qa` tag; it never matches titles, dates, or other user-entered fields. Ordinary test fixtures remain confined to test source sets and are not app data.
- **Consequences:** Fresh installs show onboarding and the real empty states with no generated Moments. Existing development databases lose the known retired fake records on upgrade while all other Moments remain untouched. No schema, dependency, production feature, or user-data reset is introduced.

## ADR-0075 — Keepsake PDF and view-only portable archives share one export boundary

- **Date:** 2026-09-11 · **Status:** Accepted
- **Context:** People need a beautiful durable artifact of their memories and a way to carry a complete Relive library to another installation without weakening the local-first model or confusing export with Google Drive restoration. The two outputs have different reading purposes, privacy consequences, and platform integration needs. Export also cannot make the Home root hydrate the archive or allow an untrusted ZIP to touch live persistence.
- **Decision:** Profile → Export creates two unencrypted Pro outputs. Keepsake PDF is an A4, oldest-first magazine of All moments or one custom timeline with optional inclusive local dates and a temporary cover photo. `.relive` v1 is a ZIP STORE container with a hashed manifest, portable archive graph, and content-addressed source media; it always contains the complete archive. Opening `.relive` is free and validates identity/version, paths, sizes, hashes, ids, relationships, and media inventory before creating a temporary read-only session. The portable viewer reuses Moment/media presentation while exposing no live repository or mutation control. App Lock remains outermost; external opens bypass onboarding without completing it. Platform system PDF/render, picker, save/share, and incoming-document facilities sit behind shared contracts. No database migration, network request, analytics, backend, or third-party runtime dependency is added, and the Drive backup format is unchanged.
- **Consequences:** `MomentRepository.listAll()` is used only during an explicit foreground export rather than by a root UI. Export creation checks `mayExport()` once at start and may finish after authorization. Missing source media fails preflight; poster/waveform extraction may use designed fallbacks. Portable media is session-local and lazy, sessions/stale files are removed, and Back unwinds media → timeline → archive index → preserved app destination. Exported files can reveal text, media, and readable locations outside App Lock, so the creation screen carries an explicit disclosure and native destinations remain user-controlled. Import, merge, restore, a recent-files library, and permanent extracted copies remain out of scope.

## ADR-0076 — Exports are timeline keepsakes with a paper-diary PDF

- **Date:** 2026-09-12 · **Status:** Accepted · amends ADR-0075
- **Context:** The complete-library portable archive and editorial magazine did not preserve the intent of sharing one chosen chapter of a life. The product owner explicitly requested that a custom timeline exported within a range keep its own name, wallpaper, and cover for the recipient, and that PDF reading feel like a hand-kept diary rather than an app timeline or media catalogue.
- **Decision:** The Export screen's scope and inclusive local date range govern both outputs. `.relive` v1 now contains only the selected All/custom timeline slice and freezes an explicit exported-timeline identity (name, wallpaper, Moment theme, custom timeline id when present, and cover reference); its opening cover and Moment surface use that identity. Keepsake PDF becomes an A4 paper diary: every observed date starts a page, the next date cannot share it, one short Moment is centered, multiple Moments flow and may continue, and each uninterrupted missing-date run between observed dates is represented by one quiet interlude page rather than many blanks. The date and Moment copy use the bundled Kalam handwriting family in dark ink directly on ruled paper, without entry cards; varied sepia vector doodles remain deliberately lighter than the writing. Image attachments use count-specific taped-photo scrapbook layouts, preserving readable scale and continuing large sets rather than collapsing them into thumbnails. Audio and video are absent. The editable one-off PDF cover remains independent from the timeline's stored cover.
- **Consequences:** ADR-0075's complete-archive/index viewer, magazine rail/year dividers, PDF video-poster/audio-waveform behavior, and card-backed Moment entries are superseded. Kalam Regular and Bold are bundled offline under SIL OFL 1.1 alongside their license so Android and iOS render the same searchable diary typography without network access. Portable source media of every supported type still travels with the selected slice and remains playable read-only. PDF preflight checks referenced images and its temporary cover only, so an unavailable audio/video attachment cannot block a format that will not print it. The archive is still unencrypted, validated, session-local, non-importing, Pro-created, and free to open; Drive backup remains the only complete restorable package.

## ADR-0077 — Opening portable archives is a first-class Profile action

- **Date:** 2026-09-12 · **Status:** Accepted · amends ADR-0075
- **Context:** Opening a received `.relive` archive was placed at the bottom of the Export creation screen, which made a free reading action difficult to discover and incorrectly coupled it to Pro-only creation. Android file providers can also expose a custom extension as generic binary or ZIP content, so matching only Relive's canonical MIME type omitted the app from Files' Open with choices.
- **Decision:** Profile's Your Memories section gains a separate **Open Relive archive** row that launches the native document picker and opens the existing validated read-only viewer. Export now contains creation only. Android continues declaring `application/vnd.vaibhav.relive.archive` as the canonical MIME type and additionally accepts `application/octet-stream`, `application/zip`, and `application/x-zip-compressed` VIEW intents because provider MIME metadata is not reliable; the archive reader remains the authority and rejects every file that fails the v1 identity, structure, size, relationship, and checksum checks.
- **Consequences:** Opening remains free, never imports or mutates live data, and uses the same App Lock/onboarding/session-cleanup rules whether entry begins in Profile or another app. Relive may appear as an Open with candidate for generic binary or ZIP files whose providers do not preserve the custom MIME type; choosing an unrelated file produces the existing corrupt/unsupported archive error instead of reading it. No dependency, schema, entitlement, or portable format change is introduced.

## ADR-0078 — PDF photo sheets are complete, unlabelled A4 pages

- **Date:** 2026-09-13 · **Status:** Accepted · amends ADR-0076
- **Context:** Large photo sets were emitted inside one tall HTML day and Android rasterized that surface in fixed A4 slices. A continuation grid could therefore begin late, cross a physical page boundary, and show a viewer seam through photographs. Repeating the date with a “continued” label also made later pages feel mechanically generated rather than like one uninterrupted diary date.
- **Decision:** The date remains exclusive to the first page for that observed day. A short single Moment may keep its first photo composition on that page when it safely fits; every remaining composition is emitted as its own top-level A4 photo section with no date and no “continued” label. Long-copy and multi-Moment dates move all photo compositions to those isolated sections. Each frame uses contain fitting so the complete source image remains visible rather than being cropped to a fixed aspect ratio.
- **Consequences:** Android's existing section-isolation renderer now receives photo sheets as independently measurable pages, so manual A4 slicing cannot bisect their photographs or frames; iOS receives the same explicit page sections through its print formatter. Large sets still use at most four readable photographs per sheet and remain in attachment order. The archive model, source media, export scope, dependencies, and portable archive format are unchanged.

## ADR-0079 — Keepsake paper is selectable and the diary hierarchy is more expressive

- **Date:** 2026-09-13 · **Status:** Accepted · amends ADR-0076
- **Context:** The warm-paper PDF used small supporting copy, printed feelings as enum words, and left short-entry pages visually sparse. The product owner supplied a journal reference with confident organic edge illustration and explicitly requested recognizable emoji feelings, larger handwriting, fuller decoration, and selectable diary-page colors with coordinated font colors.
- **Decision:** Keepsake setup offers six one-off paper choices: Warm cream, Blush pink, Sage green, Lavender, Powder blue, and Soft peach. Each choice resolves one curated high-contrast ink/accent/rule palette automatically rather than exposing unsafe arbitrary text colors. Kalam becomes the dominant large handwriting hierarchy across cover title, date, Moment title, writing, metadata, and tags. A feeling is printed after the Moment copy and tags as a large emoji (`😊`, `🙂`, or `😔`) with a non-visible accessible label, never as `Great`, `Good`, or `Low` copy. Page decoration combines the established memory icons with larger low-contrast organic edge shapes and simple line figures; writing always stays above it.
- **Consequences:** ADR-0076's single warm-paper/dark-sepia rendering and smaller `15pt`/`10.5pt`/`8.5pt` scale are superseded. Paper choice is temporary export configuration and changes neither app appearance nor stored timeline identity. The six fixed mappings preserve predictable contrast and Android/iOS parity without adding a dependency or a free-form color picker. Pagination and emoji glyph rendering must be checked in platform fixture PDFs because larger copy can create more continuation pages and emoji fallback is platform-owned.

## ADR-0080 — Physical PDF sheets own their paper, frame, and safe inset

- **Date:** 2026-09-13 · **Status:** Accepted · amends ADR-0078 and ADR-0079
- **Context:** Screen-sliced Android continuation pages could outlive the HTML body's painted height, exposing white below the selected paper, and long writing could begin or end against a physical page edge. On a multi-Moment date, moving every photograph to later sheets also left a short image-only felt Moment looking like an emoji-only entry.
- **Decision:** The platform PDF renderer paints the selected paper beneath every physical A4 sheet and fits each HTML slice inside a safe inset. It then draws a visible palette-accent rounded double-line frame with a restrained centered diamond detail over every page. A bounded WebView-height tolerance absorbs only the small A4 measurement tail that otherwise creates an empty physical sheet. Long writing is divided at whitespace into balanced complete top-level continuation sections before platform rendering; the date-and-heading page has a smaller text allowance, continuation pages have a larger allowance matched to their additional printable height, title/time remain on the first section, and location/tags/feeling remain on the last. A short image-bearing Moment may keep its first at-most-four-photo composition inline even when other Moments share its date. Every overflow photo composition follows its owning Moment before the next Moment begins, and dedicated photo sheets use larger count-specific compositions to occupy the page without crowding it.
- **Consequences:** White renderer fallback cannot appear beneath short content, continuation writing keeps top and bottom clearance without clipping a line at a raster slice, and page framing remains coordinated with all six paper palettes. Balanced text sections avoid a needlessly sparse final continuation page and use substantially more of each unlabelled continuation sheet. ADR-0078's rule that every multi-Moment photograph must move to an isolated sheet is narrowed for the first safe composition of a short Moment; overflow sheets and long-copy photographs remain isolated but never detach from their owning Moment in reading order. No schema, dependency, export scope, media type, or archive-format behavior changes.

## ADR-0081 — PDF quality and background-safe export lifecycle
- **Date:** 2026-09-13 · **Status:** Accepted
- **Context:** PDF exports need a deliberate image-size tradeoff, and leaving the Export screen during a long foreground generation must not destroy work or its one-off cover. A screen-owned disposal callback previously cancelled generation, while completion feedback had no shared platform seam.
- **Decision:** Add `PdfImageQuality.Standard` and `.HD` to temporary `MagazineOptions`, defaulting to Standard; Standard re-encodes PDF images at a maximum 1280 px long edge and JPEG quality 70, while HD retains normalized Moment images and normalizes its one-off cover at a maximum 1920 px and quality 82. Add the shared `ExportCompletionNotifier` contract with `Ready` and `Failed` outcomes and an unavailable no-op implementation. `ExportViewModel` owns generation lifetime and tracks whether Export is visible: a terminal outcome is notified at most once and only while the screen is hidden. Disposing the screen only marks it hidden; the app-root disposal closes the view model and performs final cleanup. Back from Setup deletes an unused one-off cover and exits. Back during Processing returns to Profile while work continues; explicit Cancel export remains the only in-screen cancellation action. Reopening Export restores the current operation stage. Profile exposes the current PDF status and progress. Notifications are generic, contain no archive-derived data, use existing authorization only, and never trigger a permission prompt.
- **Consequences:** Android and iOS can post native completion notifications without platform APIs leaking into common code. A hidden export can finish and notify without duplicate alerts, while a visible result remains in the existing result UI. This lifetime is scoped to the current app process; OS suspension, activity recreation, and process-death recovery are not promised. Standard/HD changes are export-only configuration and add no persistence schema, archive-format field, dependency, or `.relive` behavior.

## ADR-0082 — Media & Storage adopts the approved archive dashboard composition
- **Date:** 2026-09-14 · **Status:** Accepted
- **Context:** Media & Storage used one summary surface followed by direct canvas rows. The product owner supplied and explicitly approved a new reference that presents the same read-only archive insights as a warmer, more visual archive dashboard, including grouped nested surfaces that conflict with the former destination-specific restriction in the design system.
- **Decision:** Media & Storage now follows the approved composition: its existing Back header and local-first introduction lead into a rounded archive hero with total stored media, Moment/file counts, and a full usage ring; a rounded Storage breakdown section contains tonal photo, video, audio, and defensive Other rows with counts, byte values, percentages, and proportional bars; a rounded Your archive section presents category counters; and a final quiet privacy card combines the local-storage disclosure with shield and botanical artwork. This destination alone may use nested tonal cards. All colors remain semantic and palette-aware, the zero-byte archive omits the ring, large text receives a stacked hero layout, and every visual value retains an accessible text equivalent.
- **Consequences:** The previous direct-canvas archive-insights composition in `DESIGN_SYSTEM.md` is superseded. The screen becomes denser and more dashboard-like by explicit approval, while remaining a read-only personal-archive surface rather than a storage-management tool. Archive measurement, missing-file handling, navigation, persistence, platform boundaries, and the prohibition on cleanup/deletion/device-wide controls do not change. No schema, dependency, or new product capability is introduced.

## ADR-0083 — Profile adopts the compact personal-archive card composition

- **Date:** 2026-09-14 · **Status:** Accepted
- **Context:** Profile presented its identity, archive counts, Pro entry, and destination rows as largely separate open-canvas elements. The product owner supplied and explicitly approved a warmer compact reference with a horizontal identity, grouped count and settings surfaces, concise row descriptions, a small Edit affordance, and simple chevrons, while explicitly requiring the existing Appearance section to remain unchanged.
- **Decision:** Profile follows that reference using the active Relive palette and existing semantic typography, spacing, radii, icon, surface, and accessibility tokens. The local photo/name interaction becomes a horizontal identity row with a dedicated Edit pill and photo badge; archive counts share one rounded surface; the Pro entry becomes one compact feature card with its action named directly; Your memories, Preferences, and Relive rows become rounded groups with tonal icon tiles and supporting copy; every row uses a shaftless `>` chevron; and the screen closes with the `Small moments. A fuller you.` signature. The existing Appearance composable and all Profile information architecture and destination behavior remain unchanged.
- **Consequences:** Profile becomes denser and more visually grouped without changing what data it reads, what any action does, entitlement policy, local identity persistence, navigation, archive loading, schema, or dependencies. Large text and touch targets continue following the existing accessibility rules even where that makes the composition taller than the supplied phone reference.

## ADR-0084 — Sunset is the fresh-install palette and launcher baseline

- **Date:** 2026-09-15 · **Status:** Accepted
- **Context:** Relive previously opened a fresh install in Warm Journal even though the product's sunset artwork and the supplied onboarding reference establish Sunset as the intended first impression. Palette changes also updated Android launcher aliases while the activity was visible, which could make a launcher stop or kill the running process; Android notifications still used a temporary bookmark vector, and iOS treated Warm Journal as the primary icon.
- **Decision:** An absent or invalid appearance palette resolves to **Sunset**. Sunset is the Android application icon and enabled launcher alias baseline and the iOS primary `AppIcon`; Warm Journal remains selectable and is preserved as `AppIconWarmJournal` plus its Android alias. Android `LauncherIconController.synchronize` coalesces the latest request and defers applying it until `MainActivity.onStop`; Android 13+ changes all aliases in one atomic `setComponentEnabledSettings` call, while older releases enable the desired alias before disabling the others. Package-manager failures are retained for retry and never escape to crash the app. The notification small icon is a final theme-neutral monochrome Relive diary/`R` mark. The four-page onboarding clause originally recorded here is superseded by ADR-0085; onboarding remains skippable and versioned, with no account or cloud-sync capability.
- **Consequences:** The installed icon now matches the fresh palette and each selected palette remains synchronized without replacing the running activity. iOS alternate icon changes continue to be platform-owned and may show the system confirmation. Existing explicitly persisted palettes remain unchanged, and no archive schema, dependency, backend, account, or cloud capability is introduced. Physical-device QA remains required for launcher refresh, notification rendering, and iOS alternate icon behavior.

## ADR-0085 — Reference onboarding override

- **Date:** 2026-09-15 · **Status:** Accepted
- **Context:** The first implementation preserved the settled four-page onboarding contract, but the product owner explicitly requested the supplied six-screen reference literally, including its branded welcome screen, five numbered pages, and optional cloud-backup explanation.
- **Decision:** Override the earlier four-page onboarding contract and its dimensional keepsake art direction. Relive now presents the reference-matched welcome screen plus five numbered pages, including the “Optional cloud backup” message. The flow always renders in light Sunset, independent of the saved or system appearance, with original scenic watercolor/photographic assets and native Compose copy, cards, controls, and privacy symbols. `Get started` advances to page one; numbered pages one through four expose Skip and circular Next; the final page exposes `Create my first timeline` and `Not now`. This is a presentation/onboarding copy override only; it does not add sync, accounts, permissions, or a backend. The onboarding version increments so existing incomplete introductions receive the new flow.
- **Consequences:** Onboarding state uses six screens, the final completion controls match the reference, and dark system appearance still shows the pale reference composition with dark system-bar icons. Product copy describes optional backup without claiming sync or implementing new infrastructure. The supplied reference remains the visual source of truth for this flow; normal app surfaces continue honoring the selected appearance.

## ADR-0086 — Ivory full-canvas reference onboarding pages

- **Date:** 2026-09-15 · **Status:** Accepted · amends ADR-0085 for onboarding presentation only
- **Context:** The approved Capture reference is materially paler than the light Sunset canvas. The existing page used the global Sunset atmospheric brush, producing a blush screen that did not match the supplied reference. The product owner explicitly approved an onboarding-only ivory exception.
- **Decision:** Onboarding paints a localized pale ivory brush (`#FBF9F6` → `#F7F4F1` → `#F4F1EE`) over the light Sunset theme tokens. This changes only onboarding's canvas presentation; global Sunset colors, saved appearance, system surfaces, and all non-onboarding screens remain unchanged. At the product owner's explicit follow-up, Capture uses the same presentation technique as Welcome: one project-owned full-canvas editorial background with the scrapbook photography and paper atmosphere blended into it, generous negative space reserved for the interface, and native Compose progress, Skip, title, body, handwritten caption, feature controls, dots, and forward action overlaid above it. Capture feature tiles retain the larger approved medallions, height, and minimum touch targets. At the product owner's next explicit follow-up, Organize adopts that technique too: a full-canvas ivory paper background integrates the layered photographic timeline-card stack, while Compose renders the progress, Skip, heading, body, card metadata, handwritten annotations, dots, and forward action above it.
- **Consequences:** The first-launch Capture and Organize pages match their references' ivory paper tone and broad editorial compositions without a runtime dependency, backend, or palette change, while avoiding the appearance of a rectangular pasted hero or separately assembled photo cutouts. Text and controls continue to use Sunset's accessible semantic tokens, and the onboarding contract remains six screens with no behavior or capability change.

## ADR-0087 — Moment-card onboarding replaces Rediscover reminder page

- **Date:** 2026-09-15 · **Status:** Accepted · amends ADR-0085 and ADR-0086 for onboarding page three
- **Context:** The product owner supplied a new approved page-three reference and explicitly requested that the “Rediscover often” notification page be removed. The replacement explains how saved memories become warm cards that can be scrolled later, using the same blended full-canvas presentation already approved for Welcome, Capture, and Organize.
- **Decision:** Numbered onboarding page three is now “Relive your moments.” Its project-owned full-canvas ivory artwork provides the paper texture and botanical edge decoration while a fixed-height, clipped viewport renders three safe sample Moments through the production `MomentCard` component. Only that card-sized viewport scrolls; the page heading, body, handwritten annotations, curved arrows, page dots, Skip, forward action, and decorative background remain fixed. The preview therefore uses the real rail, dots, pinned-paper cards, metadata, tags, feeling marks, and media placement. Its three project-owned sample photos are supplied through an explicit non-persisted preview-media slot, remain non-openable, and never enter the person's media store or archive. The sequence length, navigation, completion behavior, local-first policy, and runtime dependencies do not change.
- **Consequences:** First launch teaches the continuous-scroll archive experience instead of presenting reminders or On This Day behavior. The onboarding flow remains the welcome screen plus five numbered pages. Page three's interactive preview is deliberately isolated from persistence, media storage writes, editing, forgetting, favoriting, and the media viewer; the adjacent native heading and body retain the page's accessible meaning.

## ADR-0088 — Private onboarding adopts the blended privacy-pouch composition

- **Date:** 2026-09-16 · **Status:** Accepted · amends ADR-0085 and ADR-0086 for onboarding page four
- **Context:** The code-rendered lock-and-leaf emblem did not match the product owner's supplied “Private by design” reference or the blended full-canvas treatment now used by the preceding onboarding pages. The product owner explicitly requested page four to follow the new reference.
- **Decision:** Numbered onboarding page four uses one original project-owned full-canvas ivory artwork with a translucent memory pouch, partially visible archival cards, berry lock, concentric rings, loose leaves, soft grounding shadow, and faint edge botanicals. The asset contains no interface copy or controls. Compose renders the page number, Skip action, heading, privacy explanation, handwritten note, three feature cards and their icons, page dots, and circular Next action above the artwork. “Optional cloud backup” remains explanatory copy only and adds no sync behavior.
- **Consequences:** The privacy page now shares the integrated editorial composition of Welcome, Capture, Organize, and Relive Moments without looking like a pasted standalone illustration. Its readable content and actions remain native and accessible; onboarding navigation, persistence, permissions, local-first behavior, dependencies, and product capabilities do not change.

## ADR-0089 — Final onboarding adopts the full-canvas sunrise composition

- **Date:** 2026-09-16 · **Status:** Accepted · amends ADR-0085 and ADR-0086 for onboarding page five
- **Context:** The product owner supplied a new approved final-page reference whose continuous mountain-lake sunrise, grounded journal still life, and curved completion sheet replace the earlier scenic final treatment.
- **Decision:** Numbered onboarding page five uses one original project-owned full-canvas golden-hour photograph with layered mountains, a reflective lake, natural foliage, an enamel mug carrying the exact inscription `Good Memories Live On ♡`, a dusty-rose leather journal carrying `Relive ♡`, and overlapping instant prints. Those two inscriptions are integrated into the photographed surfaces; Compose renders the page number, completion heading and body, sky annotation, primary completion action, and `Not now` above the decorative image. The foreground remains visually continuous beneath a taller curved ivory action sheet, and the primary action retains its semantic Sunset color and native pressed behavior.
- **Consequences:** The final page now matches the same blended, edge-to-edge presentation as the rest of the approved onboarding sequence while keeping all readable and interactive content native and accessible. Completion behavior, onboarding persistence, Home routing, local-first policy, dependencies, and product capabilities do not change.

## ADR-0090 — Judge data is isolated to an explicit demo flavor

- **Date:** 2026-09-16 · **Status:** Accepted · amends ADR-0074 for the Shipaton judge artifact only
- **Context:** ADR-0074 correctly removed automatic QA data from ordinary runtime builds. Shipaton judging and a sub-two-minute demo require an immediately legible archive, while production must still begin with onboarding and an empty database. The product owner explicitly requested a real database-backed judge variant rather than UI mocks.
- **Decision:** Android has separate `demo` and `production` product flavors. Only `demo` packages original sample media and an idempotent bootstrap that writes fixed sample Moments, relationships, tags, favorites, locations, feelings, and custom timelines through the production repositories and app-owned media store. It completes onboarding for that isolated installation and uses application ID `com.vaibhav.relive.demo`. The judge artifact is `demoDebug`, because RevenueCat intentionally permits Test Store keys only in debuggable apps; `demoRelease` rejects those keys. `production` compiles a no-op bootstrap, packages no demo assets, retains application ID `com.vaibhav.relive`, and rejects RevenueCat Test Store keys. All variants otherwise use the same app, database, backup, export, media, and entitlement implementations.
- **Consequences:** ADR-0074 continues to prohibit sample/QA archive data in debug and release builds of the production flavor. The judge app can coexist with production, a real newly created Moment exercises the same persistence path, and flavor tests enforce Test Store separation. Demo media is project-owned and exists only under the demo source set. No schema, backend, analytics, or fake purchase result is introduced.

## ADR-0091 — Friends testing is an empty, Test Store flavor
- **Date:** 2026-09-16 · **Status:** Accepted
- **Context:** Friends need an installable build that behaves like a fresh Relive installation while allowing real RevenueCat Test Store purchases. Reusing the judge flavor would leak seeded content, while using production would reject the Test Store key.
- **Decision:** Android has a separate `friends` flavor with application ID `com.vaibhav.relive.friends`, no bootstrap data or bundled demo assets, and a flavor-specific public RevenueCat key resolved from local configuration. `friendsShare` is the supported shareable Test Store artifact: it is release-packaged, debuggable, and signed with the local Android debug certificate because RevenueCat rejects Test Store keys in non-debuggable APKs. `friendsRelease` may be signed with the locally configured release keystore but intentionally rejects Test Store at runtime. Friends and production share all repositories, backup, restore, export, and entitlement gates.
- **Consequences:** Friends can be reset and shared without exposing judge fixtures, and friends can exercise the complete purchase/restore path through the Test Store. A private release keystore remains a local configuration concern; no signing material or API key is committed. Google Drive requires an Android OAuth client for the friends package and certificate in addition to demo/production clients.

## ADR-0092 — Onboarding becomes a five-chapter continuous native scene

- **Date:** 2026-09-20 · **Status:** Accepted · amends ADR-0073, ADR-0085, ADR-0086, ADR-0087, ADR-0088, and ADR-0089 for onboarding presentation
- **Context:** The welcome-plus-five-screen onboarding accumulated separate full-canvas compositions joined by an outgoing-then-incoming fade. The product owner supplied a new five-panel reference and explicitly authorized beginning with Capture, replacing flattened page artwork with native product previews and atmospheric gradients, and carrying the same memory continuously through the story.
- **Decision:** The visible onboarding has exactly five chapters: Capture, Organize, Relive, Privacy, and Ready. A single shared-Compose scene keeps the background, brand region, navigation, and one stable sample-memory identity alive while chapter progress interpolates its position, size, treatment, and atmosphere. Supporting native groups reveal around it with restrained overlapping opacity, 8–12dp travel, and slight scale. Capture uses the composer structure and feature icons; Organize uses production timeline cards through an isolated non-persisted preview-cover slot; Relive uses production `MomentCard`, rail, dots, metadata, and media in a clipped scrollable preview; Privacy uses native lock/surface rows with accurate local-first and optional-backup wording; Ready reuses the same cards and retains both existing completion callbacks. All sample state stays in memory, preview controls cannot mutate or open the archive, and the current onboarding completion version remains unchanged so this visual redesign cannot re-onboard completed installations. Rapid navigation retargets from current transition progress. Reduced motion snaps spatial choreography to the stable chapter and retains only short copy/action fades.
- **Consequences:** The separate Welcome chapter and old full-canvas onboarding artwork are no longer rendered, but their historical decisions and assets remain in history. Existing-install detection, native completion preferences, App Lock priority, incoming-share/deep-link priority, Home routing, no-partial-progress rule, permissions, archive storage, and production component defaults remain unchanged. Five onboarding-only atmospheric colors are documented and do not alter global palettes. No schema, backend, account, sync behavior, analytics, runtime dependency, or real archive write is introduced.

## ADR-0093 — Onboarding follows five interpolated dark app palettes

- **Date:** 2026-09-20 · **Status:** Accepted · amends ADR-0085, ADR-0086, and ADR-0092 for onboarding color presentation
- **Context:** ADR-0092 retained a fixed light Sunset token environment and introduced five onboarding-only pastel atmospheres. The product owner explicitly requested the onboarding chapters to use real app dark-mode themes and for adjacent theme changes to become visually imperceptible rather than read as screen-level palette swaps.
- **Decision:** Capture, Organize, Relive, Privacy, and Ready use the existing dark Evergreen, Original, Plum & Gold, Sunrise, and Sunset palettes respectively. The sequence is chosen as a low-depth-variance hue path from teal through indigo and plum to burgundy. The shared scene interpolates canvas, glow, tint, accent, and shadow roles continuously over the same `long3` chapter progress used by the carried memory, recreating Relive's normal atmospheric canvas brush between palette endpoints. Native component semantic colors target the matching chapter palette through the existing animated `ReliveTheme`; no new palette or raw component color system is added. All chapters keep light system-bar icons.
- **Consequences:** Onboarding now previews five real selectable Relive palettes in dark mode, its background has no brush-swap boundary, and its native components change surface/accent/text roles coherently with the chapter. Saved app appearance remains untouched, completion/version behavior is unchanged, and no schema, dependency, capability, or archive write is introduced.

## ADR-0094 — Shared card-led onboarding shell

- **Date:** 2026-09-21 · **Status:** Accepted · amends ADR-0092 and ADR-0093 for onboarding presentation only
- **Context:** Capture and Organize were refined into a continuous card-led scene, but the previous onboarding specification still described an older circular-forward-action shell. Without a documented shared frame, the later chapters could diverge in progress, navigation, spacing, or surface treatment.
- **Decision:** All five onboarding chapters use the shared shell in `ONBOARDING_DESIGN.md`: one interpolated dark atmospheric canvas, compact header, generous native visual stage, and a lower shell with five segmented progress, heading, concise caption, primary forward action, and contextually available Back. The carried square sample Moment retains one focused position and size across Capture → Organize; timeline chrome appears during its handoff, and the Organize preview uses smooth focused auto-scroll. Relive, Privacy, and Ready keep their already-approved content, actions, palette order, privacy constraints, and completion behavior, but extend this shell rather than creating alternate page layouts.
- **Consequences:** Later onboarding work has a stable presentation contract while Capture and Organize retain their approved appearance. No onboarding chapter is added, removed, or implemented by this decision; there is no change to completion persistence, permissions, archive state, dependencies, palettes, or app behavior outside onboarding.

## ADR-0095 — Rediscover collection navigation uses a direct full-screen fade

- **Date:** 2026-09-23 · **Status:** Accepted · amends ADR-0065 motion only
- **Context:** The shared container transform between Home's Material carousel cards and read-only collection surfaces repeatedly exposed transient carousel mask and layout positions, producing a visible ghost collapse and neighboring-card settlement on Back. Recreating Home in the direct-fade route still made the expanded backdrop, carousel, and Mood bar enter and measure independently while the collection was fading away. The product owner explicitly requested replacing that transform while retaining Mood insights as its existing fade overlay.
- **Decision:** Opening and closing a read-only Rediscover collection uses a tokenized full-screen fade with the same direct visual character as the Mood insights overlay. Home remains composed below the collection layer for the whole route, so the route fades over the existing settled Home surface. Rediscover cards and collection destinations no longer register or consume shared bounds, and no carousel measurement participates in route motion. Mood insights remains the full-screen scrollable fade overlay specified by ADR-0066. Collection destinations, carried covers, navigation callbacks, Back behavior, and reduced-motion support remain unchanged.
- **Consequences:** ADR-0065 remains authoritative for Rediscover hit testing, collection ordering, sliding-cover presentation, and carrying the tapped card's cover, but its container-transform clause is superseded. Custom timeline cards continue using ADR-0063's container transform. The Rediscover route no longer depends on the source card remaining composed or retaining a stable mask during navigation, eliminating first-open and return-only geometry divergence without delays or alternate first-open behavior.

## ADR-0096 — Empty custom timelines use a theme-aware landscape cover

- **Date:** 2026-09-24 · **Status:** Accepted
- **Context:** Custom timelines without a chosen cover photo used a neutral gray media placeholder. The onboarding's custom timeline card instead establishes the desired abstract landscape language while remaining visibly connected to the surrounding palette.
- **Decision:** A custom timeline with no selected cover photo renders the shared abstract landscape thumbnail on both its Timeline Home card and its detail cover. The thumbnail uses a sky, sun, layered mountain, and foreground composition matching the onboarding custom-card family, with colors derived from the active Relive semantic palette. Explicit cover photos still take precedence. The All Photos no-cover placeholder and generated collection covers do not change.
- **Consequences:** Empty custom timelines retain a visually continuous source/destination cover for their container transform and no longer look like missing media. No cover image is persisted or fabricated, and there is no schema, media, dependency, or archive-behavior change.

## ADR-0097 — Rediscover cards rotate preview photos hourly

- **Date:** 2026-09-24 · **Status:** Accepted · amends ADR-0064 cover selection
- **Context:** Rediscover cards were re-dealt only once per app launch and could remain generated gradients even when their bounded collection projections contained photos. The product owner requested random photos that change hourly.
- **Decision:** Every Home Rediscover card selects exactly one image from its existing bounded preview projection. A stable collection-specific starting offset plus the current epoch-hour bucket makes the choice stable through recomposition, scrolling, navigation, and configuration changes within an hour and advances it at each hour boundary when at least two distinct candidates exist. Images alone are eligible; video and audio are excluded. A collection with no eligible image uses the accent-derived generated fallback. Home refreshes the bucket at the boundary and on resume. The resolved cover is still carried into the opened collection surface.
- **Consequences:** ADR-0064's per-launch shuffle is superseded by hourly photo rotation for the Home Rediscover row. No archive-wide read, persistence, schema, repository API, dependency, or user-selectable cover is introduced; the candidates remain bounded by the existing Rediscover projections. A zero- or one-photo collection cannot visibly rotate until another eligible preview enters its bounded projection.

## ADR-0099 — Media can be saved back to the platform library

- **Date:** 2026-09-24 · **Status:** Accepted
- **Decision:** Images and videos may be copied from Relive's app-owned archive into the user's platform media library. The full-screen viewer exposes a one-tap action for its current item. All Photos and the per-Moment media gallery use Material 3 contextual selection: long-press enters selection, subsequent taps toggle items, and contextual actions provide Select all, Cancel, and Download. Completion is reported with a snackbar. Android writes through MediaStore; iOS writes through Photos with add-only usage disclosure.
- **Consequences:** Downloading never moves, deletes, or changes Relive's original file or Moment data. A batch can report partial failure. Audio remains outside this visual-media download action. No schema, backend, sync, sharing flow, or new runtime dependency is introduced.

## ADR-0101 — Saved Moments embed time and use soft-blur markers

- **Date:** 2026-09-24 · **Status:** Accepted · amends the approved timeline reference for saved-Moment metadata and markers
- **Context:** The reference's small dot and combined `DATE • TIME` eyebrow left location capable of disrupting the marker/date alignment and kept the timestamp visually detached from the physical-print treatment. The product owner explicitly approved superseding those settled details.
- **Decision:** Each saved Moment uses the supplied reference's soft-blur circle: a compact, slightly deepened theme-primary core that fades through progressively lower opacity to a transparent edge, centred on the rail with no glyph, border, or hard perimeter. An explicit content gap separates the marker from the abbreviated `MMM d, yyyy` chip, which uses a light translucent highlight-accent fill and border. Marker, date, and favorite occupy one fixed 48dp top row and share its centreline. Optional location renders in a separate row with its own top gap, so it never changes that geometry. Time leaves the external metadata row and appears as low-emphasis text at the print card's bottom-right. The composer plus marker and its own date/time presentation do not change.
- **Consequences:** The saved timeline marker and metadata intentionally differ from the original reference. The rail is increased from 1dp to 4dp and uses the active theme's primary → highlight accent → primary gradient through saved Moments and the composer. Card geometry, location value rules, feed order, composer behavior, persistence, and Moment identity are unchanged; no dependency or schema change is introduced.

## Template for new decisions

```text

## ADR-XXXX — <short title>
- **Date:** YYYY-MM-DD · **Status:** Proposed | Accepted | Superseded
- **Context:** …
- **Decision:** …
- **Consequences:** …
```
